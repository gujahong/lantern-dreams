# 작업일지

## 서고 재설계와 전투 레이어 (2026-09-17)

- 최신 방향과 시안을 `design/archive-reboot/`에 모았다. 기존 세계관은 큰 흐름의 참고이며 서고 구조는 새로 설계한다.
- 사람이 없는 거대한 도서관 홀을 몰입 중심 튜토리얼로 사용한다. 장애물로 탐색을 유도하고 읽을거리를 모두 읽으면 열리는 장치를 둔다. 구체적인 장치는 미정이다.
- 사용자가 선택한 전투 구도를 바탕으로 등불과 적이 없는 배경, 실제 알파 투명도의 적 PNG, 합성 미리보기를 만들었다. 고리 사이의 체크무늬도 제거했다.
- `design/archive-reboot/preview.html`은 배경을 고정하고 적만 위아래로 움직이는 브라우저 미리보기다. Godot 전투 씬 적용 및 부위별 분리는 아직 하지 않았다.
- 캐릭터 작업은 사용자 요청으로 중단했다. 새 시안은 `codex/archive-redesign` 브랜치에 보관한다.

## 인수와 세계관 읽기 (2026-09-17)

- 저장소를 이 PC의 `대화기반/game_4`에 복제했다. 기존 규칙 검사 24개가 통과했다.
- 시작 관문·조우·그것과의 전투 화면을 실제 렌더링으로 촬영했다. 결과는 저장소 바깥 `../screenshots/`에 있다. 첫 캡처 실행은 엔진 메모리 접근 오류로 종료됐고, 일반 데스크톱 권한으로 재실행해 촬영했다. 캡처 실행은 종료했다.
- 세계관·스토리라인·첫꿈·둘째꿈·셋째꿈 문서를 전체 읽고 아트 기획서와 전투 문서도 확인했다.
- **사용자 최신 설명: 세계관은 확정이 아니라 큰 흐름이다.** 문서에 '정해진 것'이나 '채택됨'이라고 적혀 있어도 현재 변경 불가능한 조건으로 취급하지 않는다. 기존 AI 제안과 사용자 발언도 구분한다.
- 기존 에셋을 최대한 활용하자는 의견과 새로 시작하자는 방향을 논의했다. 서고 재디자인 및 타일 편집 방식은 아직 논의 중이며, 새 프로젝트나 편집기 구현은 시작하지 않았다.

---

**이 파일을 먼저 읽을 것.** 컴퓨터 2대를 오가며 개발 중이라 대화 기록이 넘어오지 않는다.
"지금 어디까지 왔고, 최근에 뭘 왜 그렇게 정했고, 다음에 뭘 할지"만 짧게 담는다. 규칙과 환경
정보는 `CLAUDE.md`에 있다.

---


## 📖 글이 빛을 받아 풀리게 하고, 아트 기획서를 쓰다 (2026-09-07)

**다음에 할 일부터.** 서고 복도가 여전히 비어 있다 — 소품이 전부 홀과 열람실에 몰려
있고, 방을 1.25배로 키우면서 입구→홀 복도가 더 길어졌다. 그리고 전투 화면에
**조사 버그**가 있다: *"그것이(가) 앞을 막아섰다"* — 두 형태가 다 나온다.

### 서가를 읽는 연출을 두 단계로 바꿨다 (회원님)

전에는 **한 단계**였다. 줄 전체가 못 읽는 문자로 처음부터 깔려 있고 우리 글이 앞에서부터
그것을 먹어 들어갔다 — 그래서 *옛 글씨가 적히는 장면 자체가 없었다.*

```
1단계   못 읽는 문자가 한 자씩 써진다.        아직 아무것도 안 읽힌다
2단계   다 써진 뒤, 앞에서부터 우리 글로 바뀐다
```

**1단계의 시계는 대사 시스템 것을 그대로 쓴다**(`chars_per_second`). 그래야 즉시완성도
글자별 실측 타이밍도 규칙을 안 건드리고 따라온다. **2단계만 `DialogueUI`가 잰다** —
대사 시스템은 1단계가 끝나면 그 줄을 "다 드러났다"로 치므로, 그 뒤는 화면이 붙잡고 있어야
한다. 그래서 **풀리는 동안 스페이스는 "넘기기"가 아니라 "마저 풀기"**로 가로챈다.

**연출은 등불빛이 글줄을 훑고 지나가는 것**이다(회원님: "꽤 멋있는 간단한 연출").
빛이 닿는 옛 문자는 떨리고, 지나간 자리는 등불색에서 천천히 식는다.
**빛이 지나간 자리가 읽힌다** — 게임의 전제가 그대로 연출이 됐다. 셰이더도 노드도 안 쓰고
글자마다 `[color]` 한 겹이 전부다.

속도는 두 번 내렸다 — 타이핑 `35 → 22 → 15`, 해독 `34 → 18`. 손잡이는 `DialogueUI`의
`DECODE_CPS` · `GLOW_TAIL` · `DECODE_EDGE` · `EDGE_LIT` 넷.

### 아트 기획서를 만들었다 (회원님 — 아트를 새로 구하셨다)

**웹 페이지 넷으로 나눴다.** 꿈마다 하나씩이고 서로 링크로 물려 있다.
저장소에는 `아트기획서.md`와 `아트기획서/화면/`(사진 14장)이 있다.

```
아트 기획서   1부 스토리 · 2부 세계관 · 3부 플레이 · 부록 그리는 규칙 + 기타 레퍼런스
첫째/둘째/셋째 꿈   세계 · 이상 · 흐름 · 그려야 할 것
```

**스토리는 표가 아니라 플레이어가 만나는 순서대로 썼다**(회원님). 꿈마다 강조색이 다른데,
문서에 적힌 각 꿈의 "공기"를 색으로 옮긴 것이다 — 장식이 아니라 정보다.

**★ 레퍼런스에 내 해석을 붙였다가 걷어냈다.** 참고그림마다 "이건 ⑦번 장면이다" 식으로
꿈에 배정했는데, 정해진 것이 아니라 내 짐작이었다(회원님: *"이 꿈의 공기에 있는 일러스트는
모두 빼"*). **각 꿈에는 문서가 직접 짚어 둔 한 장씩만** 남기고, 나머지 열일곱 장은
본 문서 뒤 **기타 레퍼런스**에 캡션 없이 모아 놓았다.

### 사진 찍는 도구 — `tools/_shots.gd`

**게임을 진짜로 띄워서 찍는다.** 그래야 문서와 화면이 어긋날 수가 없다. 서가 앞으로
주인공을 옮기고, 글을 읽히고, **화면이 실제로 그 상태가 되기를 기다렸다가** 찍는다 —
시간을 재서 찍으면 속도 상수를 고칠 때마다 사진이 엉뚱한 순간으로 밀린다.

```
Godot --path . --audio-driver Dummy res://tools/_shots.tscn -- --walk
```

관문·전투는 각 씬에 이미 있는 `--capture` / `--shot`이 찍는다.

### 서고를 손봤다 (회원님)

- **벽을 통째로 없앴다.** 사면에 세워도 봤지만 바깥이 심연이라 배경이 거의 검정이어서
  **벽면이 배경에 녹고** 밝은 꼭대기 줄만 남았다 — 벽이 아니라 테두리 선으로 보였다.
  **심연 위에 놓인 판**으로 간다
- **바닥 데칼도 크랙만 남기고 뺐다**(회원님: "왜 이렇게 더럽냐")
- **장식 책장을 없앴다.** 읽는 서가와 같은 그림이라 어느 것이 읽히는지 흐렸다 —
  이제 **책장이 서 있으면 전부 읽는 것**이다
- 읽는 서가는 셋을 붙이던 것을 **하나로** 줄였다(붙여 놓으니 벽지처럼 보였다).
  `Records`의 가운데 정렬 버그도 같이 고쳤다 — `(k-1)*STEP`이라 셋일 때만 맞았다
- 방을 **1.25배**로, 걸음은 `90 → 70`, 등불은 `light_radius 0.18` / `softness 0.30`

---

## 🔦 등불의 정체를 정하고, 첫 꿈의 지형을 다시 잡다 (2026-08-24)

**받아온 8/23 세션 위에 이어서 했다.** `main`을 `claude/code-review-zkdyv0`로 앞으로 감았다
(커밋 60개). 코드보다 설정 쪽이 크게 움직였다.

### ★★★ 등불은 빛을 빨아들여 보여주는 물건이다 (회원님)

첫 꿈이 **뜨겁고 환한 세계**라 "밝히는 물건"이 쓸모가 없어지는 문제가 있었다. 회원님이
뒤집으셨다 — **빛을 내는 것이 아니라 빨아들여 보여주는 것.**

그리고 한 번 더 뒤집으셨다. 내가 *"밝은 데서는 빨아들일 빛이 넘치니 안 마른다"*로 썼는데
반대였다 — **너무 밝아서 안 보이는 것을 보게 해주는 것**이므로 눈부신 데서도 태운다.

```
너무 밝다    →  기름을 태운다   빨아들여서 본다      ← 첫 꿈의 낮
알맞다       →  안 태운다                            ← 첫 꿈의 그늘 = 밤
너무 어둡다  →  기름을 태운다   남은 빛을 모아 본다  ← 서고
```

#### 그래서 규칙을 한 단계 위로 올렸다

```
보이는 것 = lerp(세상의 원래 밝기, 알맞음, 눈금)
```

**"그늘에서 안 태운다"를 예외로 안 적어도 된다** — 세상이 이미 알맞으니 등불이 끌어올 것이
없고, 그래서 눈금이 0이어도 잘 보인다. 서고에서 눈금이 떨어지면 캄캄해지는 것과 첫 꿈에서
하얗게 타는 것이 **한 식**이다. 코드로는 세상 쪽 값 하나면 된다 — `-1` 어둠 · `0` 알맞음 ·
`+1` 눈부심.

**★ 그리고 이 규칙에서 첫 꿈의 사회가 그대로 나온다.**

| | |
|---|---|
| **행렬** | 그늘을 따라 걷는 사람들. 거기가 유일하게 등불이 안 드는 곳이다 |
| **마을** | 고정(회원님). 따라가기를 그만두고 남은 사람들 |

**"밤이 온다"가 안도가 된다** — 다른 게임이면 무서운 자리인데 여기서는 반대다.

### ★★ 맵은 도넛이 아니라 감긴 띠다 (회원님)

*"이 세계의 공간은 이어져 있어서, 직선으로만 가는데 돌아온다."*

**설정이 구현을 쉽게 만든 경우다.** `_walkable()`을 가로로 주기적이게(`posmod`) 하면
지형이 진짜로 반복되므로, 주인공 자리를 한 주기마다 감아도 **이음매가 안 보인다.**
도넛 지형을 만드는 것보다 간단하다.

8/21에 적어 둔 *"빙빙 돈다"*는 반만 맞았다 — **돌아오는 것은 맞고 도는 것은 틀렸다.**

### `첫꿈.md` 5장을 다시 썼다

8/21 초안(도넛 맵·행렬이 도는 원) 전제라 안 맞았다. **그늘이 이 꿈의 기술적 심장**이 됐다.

- **서고와 정확히 반대다** — 서고는 *어두운 세계 + 밝은 구멍*, 첫 꿈은 *밝은 세계 + 어두운
  구멍*. `DitherScreen`이 이미 한 점으로 구멍을 그리므로 **부호만 뒤집으면 된다**
- 새로 쓸 스크립트가 셋 — `Shade.gd` · `Caravan.gd` · `Crumbs.gd`
- **`Shade.lit_at()` 하나를 화면도 규칙도 본다.** 참/거짓이 아니라 이어진 값이라
  그림자 언저리가 안 끊긴다. 서고는 이 값이 늘 `-1`이라 **같은 규칙이 두 세계를 굴린다**

### 문서 중복을 걷어냈다

`첫꿈.md`에 세 절(*이 세계만의 일이 아니다*·*노인은 불안정해져서*·*퍼지는 중이다*)이
**43줄 그대로 두 번** 들어가 있었다. 제목 깊이만 달랐다. ⑨가 ⑧보다 먼저 나오던 것도 고쳤다.
다른 문서는 기계로 대조해 확인했다(제목 중복 검사).

### 코드

**★ 8/23 세션의 버그 셋을 실행으로 확인했다.** *"Godot이 없는 환경이라 확인 못 했다"*고
적혀 있던 것이다. `tools/_flow.gd`(+`.tscn`)를 새로 짜서 **규칙 층 21개 항목**을 헤드리스로
쟀고 전부 통과했다. 씬 셋(서고·종이 적 조우·오프닝)도 런타임 오류 없이 뜬다.

**⚠️ 확인 도구는 `--script`가 아니라 씬으로 띄워야 한다.** `--script`로는 오토로드가
안 올라와서 `Walker`·`Records`가 아예 컴파일이 안 된다.

**★ 그런데 `Walker.forget_all()`을 아무도 안 부르고 있었다.** 정의만 있고 호출부가 없어서,
처음부터 다시 해도 서가가 읽힌 채·북쪽이 열린 채로 시작했다. `Opening._enter()`에 넣었다.

#### 등불 눈금을 세로로 세웠다 (회원님)

가로로 눕혀 등불 밑에 깔았더니 **부챗살의 맨 아래 줄기(도주)와 자리싸움**이 났다. 그것을
피하려고 부챗살 각도를 **세 번 갈아엎었고 전부 헛수고였다.**

| 해본 것 | 무너진 곳 |
|---|---|
| 범위를 `0도`까지 좁히기 | 줄기 사이가 좁아져 **빛 원뿔끼리 파고들었다** |
| 세로를 고르게 나누기 | 맨 위가 등불 바로 위에 서서 **혼자 튀었다** |
| 원뿔을 좁히기 | 원뿔이 글자를 못 품어 **글자가 빛 밖으로 나갔다** |

**답은 눈금을 세로로 세워 옆으로 비키는 것이었다**(회원님). 등불 밑이 비니 부챗살은 원래
값 그대로다. **자리가 모자라면 있는 것을 옮기지 말고 새 것을 비켜 놓을 것.**

**★ "겹친다"가 글자인지 빛인지 먼저 확인할 것.** 회원님은 빛 원뿔을 말씀하셨는데 나는
글자로 알아듣고 계산 방식을 두 번 갈아엎었다. 나중에 검은 바탕에 메뉴만 그려 각도별 밝기를
재보니(`tools/_menu.gd`) **줄기 폭은 다섯 다 똑같았다** — 내가 눈으로 보고 "아래가 두껍다"고
한 것도 틀렸다.

#### 고른 줄기는 통째로 커진다 (회원님, "가시성을 위해")

**길이만 늘리면 소용이 없다.** 원뿔은 길어질수록 같은 자리에서 오히려 얇아진다 — 재보니
글자 자리에서 `39.6` 대 `39.8`로 사실상 같았다. **길이와 너비를 같은 비율로 키우면**
차지하는 각도가 안 바뀌어서 옆 줄기를 안 건드리면서도 눈에 띈다.

#### 전환이 "뚝" 끊기던 것도 고쳤다 (회원님 제보)

`BattleStage._process`가 앱 켠 시각을 위상으로 써서, 자리를 다 잡는 프레임에 `sin(now)`이
아무 값이나 되어 **등불과 적이 한 번 툭 옮겨 앉았다.** 앉은 시각을 0으로 잡으니 멎었다.
**주기적으로 흔드는 것은 시작 시각을 따로 잡을 것.**

### 새로 생긴 확인 도구 셋

```
tools/_flow.gd + .tscn    규칙 층 21개 항목을 헤드리스로 잰다
tools/_menu.gd + .tscn    빛줄기만 검은 바탕에 그려 각도별 밝기를 숫자로 뽑는다
tools/_crop.gd            화면 한 귀퉁이를 잘라 키운다
```

**화면에서는 필터·어둠·디더가 섞여서 눈으로 못 가린다.** 눈으로 본 것을 그대로 말하지 말고
재볼 것 — 이번에 두 번 틀렸다.

### 다음에 할 것

- **첫 꿈 1단계** — 그늘만 걸어본다. 타일셋은 서고 것 임시로 → 비용 0
- **그것의 대사** — `resources/watcher.tres`가 아직 설정 서기 전 것이다
- **이름들** — 세계·그것·노인·박사·아이·배 전부 이름이 없다
- **배신자를 언제 만나는가** — 꿈 셋이 아직 따로 논다

---


## 📖 세계관을 다시 세우고 꿈 셋을 짰다 (2026-08-23)

**이 세션은 거의 다 스토리다.** 코드는 버그 셋을 잡은 것뿐이다.
꿈 하나하나는 `첫꿈.md` · `둘째꿈.md` · `셋째꿈.md`에 있고, 세계의 규칙은 `스토리자료.md`에
모아 뒀다.

### ★★ 앞선 세션이 지어낸 것을 정해진 설정으로 적어 놨었다

**이 세션에서 회원님께 여러 번 지적받은 것이다.** 8/21 세션이 *"따라 나온 것"*으로 추론한
말들이 문서에 정해진 것처럼 박혀 있었고, 나도 그 위에 또 얹었다.

| 걷어낸 것 | |
|---|---|
| **"재운다"의 대상** | 그것을 재우는 것이 아니라 **초월적 존재**를 재우는 것이다(회원님) |
| **"가라앉힌다"** | 출처가 없는 말이었다. 전부 뺐다 |
| **"걷는 것이 무엇의 이치인지 아무도 모른다"** | 폐기. **곧 낮과 밤이다**(회원님) |
| **첫 꿈의 마을 실마리 넷** | 눈금·물건 더미·부스러기·노인 대사 전부 초안이었다 |
| **서고 그것의 대사** | *"나는 결사의 수호자다"*는 **주인공 말**이다(회원님). 그것은 아무 말도 안 한다 |

**앞으로 지킬 것** — 회원님이 주신 것에는 (회원님)을 달고, 내가 지어낸 것은 *(내 제안)*이라고
적는다. 표시 없는 서술문은 초안으로 본다. 문서마다 머리말에 이 규칙을 박아 뒀다.

### 세계관에 새로 박힌 것

- **★ 이치에 벗어날수록 꿈이 불안정해진다**(회원님). 결사가 하는 일은 이치를 제자리로
  돌리는 것이고, 방법은 **바로잡거나 · 살리거나 · 죽이거나**.
  **이 규칙 하나가 "결사가 균열의 원인"과도 이어진다** — 그들이 이치에 안 맞는 존재라서다
- **★ 깨어남은 사건이 아니라 과정이다**(회원님). 존재가 깨어나 가면서 꿈들이 하나씩
  사라지고, **가장 불완전한 꿈부터** 간다. 그래서 이야기 전체가 시계가 된다
- **★ 서고도 꿈이다 — 중심의 꿈**(회원님). 가장 안전하지만, 다른 꿈들이 무너지면
  서고도 위험해진다
- **★ 지금 거의 모든 세상이 아파간다. 존재 자체가 불안정한가 보다**(회원님)
- **이치 밖으로 나간 것은 무엇이든 괴물이 될 수 있다**(회원님). 사람만이 아니다
- **"그것"은 이름이 아니라 부르는 방식**(회원님). 세상마다 하나씩 있다.
  표기는 붙여 쓴다 — 저장소 전체 38곳을 맞췄다
- **결사의 목표는 계속 안 밝힌다.** 이유가 생겼다 — **세상마다 선택지가 있어서**다(회원님)
- **서고의 그것은 문지기다.** 이방인을 막고 결사대만 통과시킨다. 아무 말도 안 하고
  계속 쳐다보다가, **대화든 전투든 마무리하면 만족하고 떠난다**

### 꿈 셋이 섰다

```
        끝          무게        분위기              싸움
첫째    고른다      몰랐다      황량하다. 야외      노인 하나
둘째    못 한다     도왔다      거의 전부 배 안     괴물 박사
셋째    한다        사랑했다    사람 사는 나라      버티기
```

- **첫 꿈** — **처음부터 다시 짰다.** 축은 *의문이 그것에게 옮겨간다*. 그늘이 그것 하나뿐인
  세계이고, 그것이 지나갈 때가 밤이다. 마주섬에서 그것이 묻는다 —
  **"이것이 꿈인가?"** 진실을 말하면 영원히 멈추고 쉬고, 거짓을 말하면 다시 움직인다
- **둘째 꿈** — 회원님 아이디어(목숨으로 가는 지상전함). **이성이 녹으면서 나오는 에너지가
  연료**이고, 창에 앉히고 묶고 가장자리를 보여주는 것이 그 방법이다.
  **마지막 연료는 박사 자신이었고 본인도 알고 있었다.** 실패로 끝난다
- **셋째 꿈** — 회원님 아이디어(승천식과 어린 왕). **드라마 담당**이다.
  주인공만 그를 아이로 대하고, 그것이 그를 괴물로 만든 원인이 된다.
  막는 방법은 죽이는 것이 아니라 **끊긴 의식을 마저 끝내는 것**

### 코드 — 버그 셋

**★ 전투가 끝나도 안 돌아가고 있었다**(회원님 제보). `BattleStage.over`를 **아무도
받고 있지 않았다** — `emit`은 있는데 `connect`가 저장소에 하나도 없었다.
`Encounter`가 받아 서고로 돌아가게 했다.

**고치니까 같은 종류가 둘 더 나왔다.** 전투에서 돌아오면 방이 새로 지어지면서 진행이
통째로 사라진다.

```
잡은 종이 더미가 도로 누워 있다      →  Sleepers.beaten을 static으로
그것과 다시 붙는다                   →  Walker.heart_done을 static으로
읽은 서가가 지워지고 북쪽이 다시 막힌다 →  Records.read_shelves를 static으로
```

새 판을 시작할 때 지우는 `Walker.forget_all()`을 한 군데로 모았다.
**⚠️ Godot이 없는 환경이라 실행 확인을 못 했다.** 받아서 한 번 돌려볼 것.

그 밖에 `EnemyDef.victory_line`을 더했다 — 적이 곧 관문일 때 이겨도 *"길이 열렸다"*가
나와야 해서다.

### 다음에 할 것

- **넷째 꿈** — 신비롭고 코즈믹 호러 쪽으로 가기로 했다. 안 쓴 소재가 물과 밤이고,
  *"세계 자체가 처음부터 이상하다"*가 앉을 자리다
- **"누군가를 살린다"에 해당하는 꿈** — 이치를 돌리는 방법 셋 중 하나가 비어 있다
- **계단의 방** — 구슬 하나가 꿈 하나라는 것 말고 통째로 비어 있다
- **`첫꿈.md`의 "만드는 법" 장** — 8/21 초안(행렬·마을) 전제라 다시 봐야 한다
- **지금 게임에 박힌 그것의 대사** — 설정이 서기 전에 쓴 것이라 다시 쓴다
- 있으면 좋겠다고 이야기만 나온 것 — **결사의 규정집**(서가에 제7항·제12항이 있다),
  **시간**(꿈에서 며칠을 보내는가), **세 꿈을 묶는 공통된 것 하나**


## 🕯️ 등불을 마나로 갈아엎었다 (2026-08-21) — 규칙은 `전투.md`

**밝기 하나가 체력이자 자원이던 것을 둘로 나눴다**(회원님). 하스스톤인데 크리스탈이 늘어나는
게 아니라 **2턴마다 하나씩 줄어든다.** 자세한 것은 `전투.md`에 있고, 여기는 **화면에서 데인
것**만 남긴다.

### 옛 기름 막대는 원래부터 안 보이고 있었다

등불이 `(44, 428)`인데 막대는 84를 더 내린 `556`에 있었다. **화면이 540이다.** 세상이
어두워지는 것이 눈금 노릇을 하고 있어서 아무도 몰랐다. 새 눈금을 붙이고 나서야 드러났다.

### ★ UI 자리싸움에서 세 번 헛돌았다

눈금을 등불 **밑**에 가로로 깔았더니 부챗살의 맨 아래 줄기(도주)와 겹쳤다. 그래서 부챗살을
만졌는데, 만질 때마다 다른 데가 무너졌다.

| 해본 것 | 무너진 곳 |
|---|---|
| 각도 범위를 `0도`까지 좁히기 | 줄기 사이가 좁아져 **빛 원뿔끼리 파고들었다** |
| 세로를 고르게 나누기 | 맨 위가 등불 바로 위에 서서 **혼자 튀었다** |
| 원뿔을 좁히기 | 원뿔이 글자를 못 품어 **글자가 빛 밖으로 나갔다** |

**답은 눈금을 세로로 세워 등불 옆에 붙이는 것이었다**(회원님). 등불 밑이 비니까 부챗살은
원래 값 그대로 두면 된다. **자리가 모자라면 있는 것을 옮기지 말고 새 것을 비켜 놓을 것.**

그리고 **"겹친다"가 글자인지 빛인지 먼저 확인할 것.** 회원님은 빛 원뿔을 말씀하셨는데
나는 글자로 알아듣고 계산 방식을 두 번 갈아엎었다.

### 그 밖에 배운 것

- **기름병을 눈금 칸과 같은 네모로 그리면 눈금이 열다섯 칸으로 읽힌다.** 가르는 금을 그어도
  소용없다 - **모양이 달라야** 다른 것이 된다. 목이 있는 병 실루엣으로 바꿨고, **빈 병은
  안 그린다**
- **눈금만 어둠 필터에서 뺐다.** 다른 UI는 불이 죽으면 같이 죽는 게 맞지만, 이것은 남은
  자원을 읽는 자리다 - 어두울수록 아껴 써야 하는데 그때 안 보이면 거꾸로다

### ★ 전환이 "뚝" 끊기던 진짜 이유

`BattleStage._process`가 `Time.get_ticks_msec()`를 그대로 위상으로 썼다. 자리를 다 잡는
프레임에 `_seated`가 켜지는데, 그때 `sin(now)`이 **하필 아무 값이나** 된다. 그래서 등불이
최대 `FLOAT_HEIGHT`만큼, 적이 `BREATH`만큼 **한 프레임에 툭 옮겨 앉았다.** 빛무리도 등불을
따라가니 화면 전체가 덜컹였다.

**앉은 시각을 0으로 잡으면**(`_seated_at`) 사인이 0에서 시작해 안 튄다.
**주기적으로 흔드는 것은 시작 시각을 따로 잡을 것.**

---

## 📖 스토리를 위에서부터 짰다 (2026-08-21) — 회원님과 정한 것

**흩어져 있던 세계관을 이야기로 세웠다.** 그전까지는 전제(꿈·결사·서고)만 있고 "그래서 무슨
이야기인가"가 없어서, 첫 장소의 글을 쓸 때마다 지어내게 됐다.

### 결사가 하는 일 — 깨우지 않는 것

```
결사의 목표는 일단 기본적으로는 깨우지 않기 위해 찾아가는 것이다.
주인공의 목표도 그럴지는 아직 미정이다.          ← 회원님 말 그대로
```

**"깨우지 않기 위해"가 비어 있던 칸을 거의 다 채웠다.** 지어낸 것이 아니라 따라 나온 것들이다.

| 따라 나온 것 | |
|---|---|
| **"고친다"가 아니라 "재운다"** | 이상은 병이 아니라 **뒤척임** — 깨려는 조짐이다. **⚠️ 2026-08-23 정정: 재우는 대상은 초월적 존재다**(회원님). 그 세계의 *그것*을 재우는 것이 아니다 — 존재가 깨어나면 세상이 붕괴하므로 안 깨우는 것이 결사의 일이다 |
| **비밀인 이유** | 알려지면 떠들고, 떠들면 소란이 난다. 취향이 아니라 업무 규정이다 |
| **왜 혼자인가** | 여럿이 건너가면 시끄럽다. 한 번에 한 사람만 넘어간다 |
| **등불이 왜 작은가** | 깨우지 않고 보는 도구라서다 |

### 등불 규칙은 안 바꾼다 (회원님)

설정대로면 밝히는 것이 위험해야 맞다. **그래도 규칙은 지금대로 둔다** — 밝기는 순수하게
자원으로 남고, 설정은 "등불이 왜 작은지"를 설명하는 데까지만 쓴다. 지금 전투가 안 깨진다.

### "등불이 비추는 것만 실재한다"는 상징이다 (회원님 정정)

**규칙이 아니다.** 조항으로 쓰면 안 비춘 것이 정말 없어지는 세계가 되고, 그러면 설정이
마법 체계로 굳는다. 글에서는 **결사 사람들이 새겨 두고 인용하는 옛말**로만 나온다.

### 🩸 균열과 깨달음 — 괴물이 어디서 오는가 (2026-08-21, 회원님이 정하신 것)

```
이상이 생기면 세상에 균열이 생긴다.
그 세계 사람 중 누군가는 여기가 꿈이라는 것을 깨닫는다. 의문을 갖는다.
그러다 괴물이 된다 — 꿈에 잡아먹히거나, 미치거나.
```

**이것이 여러 구멍을 한꺼번에 메운다.**

- **적이 어디서 오는지 정해졌다.** 종이로 된 것도 빈 갑옷도 그냥 있던 것이 아니라
  **원래 그 세계 사람이었던 것**이다. 깨닫고, 미치고, 잡아먹힌 것들
- **결사가 왜 급한지 설명된다.** 이상 자체보다 **균열이 사람을 깨닫게 하는 것**이 문제다
- **공포의 정체가 정해졌다.** 알아차리는 것이 곧 파멸이다

#### ★★★ 균열의 또 다른 원인은 결사단이다 (회원님)

```
다른 세상에서 무언가가 흘러 들어오거나 자주 섞이면 세상이 더 불안정해진다.
다른 세상의 사람은 그 세상 것이 아니니까 세상의 이치가 이상해진다.
그 흘러들어오는 존재가 바로 결사단이다.
```

**결사가 소방수이자 방화범이다.** 고치러 갈수록 그 세계가 흔들리고, 꿈이라는 것을 아는
사람이 늘어난다.

**이것이 여러 개를 한꺼번에 푼다.**

- **"주인공은 왜 안 미치나"의 답.** 주인공은 그 세계 사람이 아니라 **그 세계의 이치에
  안 묶인다.** 깨달아도 안 미친다. 대신 —
  > **주인공은 안 미치는 대신, 있는 것만으로 남을 미치게 한다.**
- **왜 혼자인가 (진짜 이유).** 앞서 "여럿이 가면 시끄러워서"라고 적었는데 그것보다 이쪽이다 —
  **적게 들어가야 덜 오염된다.** 한 번에 한 사람만
- **왜 비밀 결사인가.** 알려지면 안 되는 것이 **자기들이 원인**이라서다
- **등불도 이물질이다.** 그 세계 것이 아니다. 그 불빛 안에서만 세상이 또렷한 것이 상징이
  아니라 **실제로 이질적인 물건이라서**가 된다

**결사의 진짜 목표는 여전히 안 밝힌다.** 다만 *"자기가 원인인 것을 알면서 왜 계속 다니는가"*가
그 자리를 훨씬 무겁게 만든다.

#### ★★★ 결사단은 원래 많았다 — 그리고 한 명이 나머지를 죽이고 있다 (회원님)

```
결사단이 원래는 많았다.
이것을(자기들이 원인이라는 것을) 깨달은 단원 하나가,
결사를 줄이려고 세상을 넘나드는 단원들을 '사고'로 죽이기 시작했다.
주인공은 그 단원을 빼면 남은 유일한 결사단이다. 본인은 모른다.
```

**이것이 서고에 아무도 없는 이유다.**

작업일지에 *"왜 수호자는 혼자인가 — 사람을 안 그리기로 했으므로 다른 단원이 화면에 안
나온다"*라고 적어 두었었다. **기술적 편의였던 것이 사건이 됐다.** 거점이 텅 빈 것은 안 그린
것이 아니라 **다 죽었기 때문**이다. 필터가 회색인 이유를 설정으로 만든 것과 같은 수법이다.

#### 첫 꿈이 전체 이야기의 축소판이 된다

```
노인      깨달았다 → 미쳤다   → 괴물이 됐다
배신자    깨달았다 → 안 미쳤다 → 사람을 죽이기 시작했다
```

**같은 깨달음의 두 결말이다.** 결사단은 그 세계 사람이 아니라 안 미친다. 첫 꿈에서 노인을
베고 나면, 나중에 배신자를 만났을 때 **그 장면이 되돌아온다.**

**그리고 배신자가 옳다.** 결사가 원인인 것이 사실이니까 단원을 줄이면 세상이 정말로 덜
흔들린다. **틀린 말을 안 하는 악당**이라 주인공이 반박을 못 한다.

보류해 둔 **주인공의 목표**가 갈 자리도 여기서 분명해진다 — **막을 것인가, 동의할 것인가.**

#### 따라 나오는 것들

- **'사고'는 세계를 건너는 순간에 난다.** 관문·포탈이 취약한 자리다. 이미 만들어 둔 `Portal`이
  그 무대가 된다
- **주인공도 언젠가 표적이다.** 마지막 하나 남았으니 다음 차례다
- **서고 장부의 오른쪽 칸이 비어 있다**(`RecordText.gd` 초안) — 지어낸 것이었는데 이제
  사실이 됐다. 나간 사람 이름은 있고 돌아온 사람 이름이 없다
- **⚠️ 서고의 *그것*이 누구인지 후보가 생겼다.** 배신자인가, 아니면 다른 것인가.
  아직 안 정한다

#### 첫 꿈이 여기서 뒤집힌다

④에서 힘들게 알아낸 결론이 *"원인이 없다. 그냥 늙었다"*이고 그것은 맞다. **그것이 느려진
것은 아무 탓도 아니다.**

**그런데 노인이 깨달은 것은 주인공 탓이다.** 주인공이 도착했고, 그 세계가 흔들렸고, 그래서
노인이 알아차렸고, 그래서 ⑤에서 괴물이 된다. **주인공이 안 왔으면 노인은 그냥 늙어 죽었다.**

그러면 ⑧이 폭발한다 — *무엇을 적고 무엇을 안 적을 것인가.* 결사의 기록에
**"내가 왔기 때문에 한 명이 죽었다"**를 적을 것인가.

### 이상은 깨어남이다 (회원님)

**꿈에 생기는 "이상" = 초월적인 존재가 깨어나는 것.** 주인공은 그 이상을 풀러 간다.
숨기는 설정이 아니라 전제다 — 숨기는 것은 여전히 **결사의 목표** 하나뿐이다.

### ★ 꿈 하나가 독립된 단편이다 (회원님)

**각 꿈은 그 자체로 하나의 이야기다.** 그 세계에서 이상이 생겼고, 그걸 풀러 간다 — 시작과
끝이 그 안에 있다. 앞 꿈을 몰라도 되고, 순서가 자유로운 것과도 맞물린다.

**그래서 "넷 다에 같은 흔적을 흘린다"는 안과 부딪힌다.** 실이 꿈 안으로 들어가면 단편이
아니라 연재가 된다.

**실은 꿈 밖에 둔다.** 꿈은 완전히 독립으로 두고, **돌아와서 적는 기록에만** 실을 건다.
쌓이는 일은 서고에서만 일어난다 — 단편집인데 서문이 조금씩 자라는 모양이다.

### 정서 넷은 게임 전체의 공기다 (회원님 정정, 2026-08-21)

**순례 · 경외 · 공포 · 신비로움을 꿈마다 하나씩 나눠 맡기는 것이 아니다.** 게임 전체가 저
공기이고, **모든 장소가 넷을 다 노린다.** 세계관 메모의 *"장소마다 노릴 것"*은 그런 뜻이었다.

그래서 **꿈을 넷으로 잡을 근거는 없어졌다** — 정서가 넷이라 넷으로 뒀던 것이라, 갯수는 다시
정해야 한다.

### 여정 — 아무 꿈이나 먼저 (갯수 미정)

```
서고(거점) ─ 포탈 ─ 계단의 방(마을) ─ 꿈 여럿 (자유 선택)
  ↑
  └── 끝내고 돌아와 적는다 → 서가가 하나 는다

꿈 안에서:  탐색(탑다운) → 이상의 자리 → 조우 연출 → 전투 → 이치를 제자리로
```

**자유 선택이라 두 군데가 샌다. 설정 안에서 막았다.**

1. **난이도가 평평해진다** → 꿈이 어려운 게 아니라 **갈수록 전체가 나빠진다.** 시간이 갈수록
   뒤척임이 심해지므로 **두 번째로 간 곳은 어디든 두 번째만큼 험하다.** 순서를 안 정해도
   곡선이 생긴다
2. **이야기가 안 쌓인다** → 꿈은 단편으로 두고 **돌아와 적는 기록에만** 실을 건다.
   어느 순서로 봐도 서고 쪽에서 모이고, 넷째쯤 그림이 맞춰진다. 그리고 **주인공의 목표를
   미정으로 둬도 양쪽 다 받는다**

### 진행도가 오르면 적이 강해진다 — 그것뿐이다 (회원님)

**만들 것을 늘리지 않는다.** 진행도마다 적을 다른 상태(누워 있다/일어선다/먼저 온다/같이
온다)로 굴리자는 안이 나왔는데, 그러면 **적 하나가 넷이 된다** — 그림도 연출도 판정도 따라
늘어난다. 회원님이 잘랐다.

**그냥 강해진다.** `EnemyDef`의 수치(`max_hp`·`damage_min/max`·`heavy_chance`)를 진행도로
올린다. 적 리소스도 그림도 그대로고, 곡선만 생긴다.

### 진행도를 방의 풍경으로 (제안, 확정 아님)

꿈 하나를 끝낼 때마다 **서가가 하나 는다.** 거점이 눈에 보이게 채워지고, 이미 만들어 둔
서가 읽기 장치가 **초반 관문이자 후반 수집물**로 두 번 일한다.

### 서가 넷의 말투가 정해졌다

**주인공은 다 알고 있고, 모르는 것은 플레이어뿐이다**(회원님). 그러니 서가 글은 설명문이면
안 된다 — 그냥 **기록**이고, 플레이어는 어깨너머로 읽는다. 항 번호가 붙고 칸이 있고 지운
자국이 있는 **사무적인 글**이어야 경외가 산다.

이것이 해독 연출과도 맞물린다. **못 읽는 것은 주인공이 아니라 우리다.** 주인공은 결사의
문자를 읽을 줄 알고, 글자가 풀리는 것은 화면 밖의 눈이 따라잡는 것이다.

넷이 답하는 것: ①여기가 무엇인가 ②등불은 무엇인가 ③무엇으로부터 지키나 ④왜 혼자인가.
글은 `scripts/map/RecordText.gd`에 있다. **결사의 목표는 넷을 다 읽어도 안 나온다.**

### 🐘 첫 꿈 — 걷는 것 (2026-08-21, 회원님이 정하신 것)

**회원님이 참고그림 열일곱 장을 주셨고**, 그중 하나를 첫 꿈으로 고르셨다 —
**분홍-보라 사막을 걸어가는 거대한 것, 그 뒤를 따라 걷는 순례 행렬, 모래의 발자국.**

```
참고그림/a35d7b9b0433424595d5a1d1561cfc67.jpg   ★ 첫 꿈. 사막의 걷는 것
참고그림/b877294f73d8ff173da7902f3c759713.jpg     격자 칸 + 붉은 눈 + 못 읽는 글자표 + 계단 (서고 쪽)
참고그림/57a46c6d28d1510ca9e32a1c6f5ac14b.jpg     사슬과 수평선, 빛 고리
```

**파일명이 해시라 다음에 못 찾는다.** 위 셋이 2026-08-21에 들어온 것이고, 나머지는 8월 10~11일
것이라 채팅에서 보여주신 것과 겹치는지는 확인 안 됐다. **채팅에만 있고 아직 파일로 안 들어온
것들:** 잠든 얼굴 판화, 눈 달린 피라미드, 검은 도시선, 소용돌이 탑, 팔 소용돌이, 1비트 사슴.

#### 그림 열일곱 장에서 나온 것

**중심축은 화풍이 아니다.** 흑백 판화·초현실 유화·레트로 SF가 다 섞여 있는데 한 그림으로
보인다. 공통된 것은 **구도와 소재**다.

> **거대한 것이 있고, 그것은 자고 있거나 보고 있고, 작은 사람이 그 앞에 서 있거나
> 그쪽으로 걸어간다.**

그래서 **꿈마다 화풍이 달라도 게임이 안 흩어진다.** 저 구도만 지키면 된다.

- **눈이 열일곱 장 중 여섯 장에 나온다.** 우연이 아니라 이 세계의 시각 언어다 — 거대한 것에는
  눈이 있고 그 눈은 공격하지 않고 본다. 서고의 *그것*이 그래서 맞았다
- **색을 적게 쓰고 세게 쓴다.** 순수 흑백 / 빨강과 파랑만 / 분홍과 보라. 회색 필터 + 주황
  등불과 같은 사고방식이다
- **사람이 계속 나오는데 전부 멀리 있는 실루엣이다.** *"사람을 안 그린다"*는 등장인물 아트를
  말한 것이지 지평선의 행렬까지 막는 것이 아니다. 도트 서너 개짜리 점이라 값이 거의 안 든다
- **그림은 다 넓고 밝은데 지금 게임은 등불 반경만 보인다** → 이것이 답이 된다.
  **거점은 좁고 어둡고, 꿈은 넓고 밝게.** 서고를 나와 시야가 확 열리는 것 자체가 사건이 된다

#### 이 세상의 이치 (회원님)

**겁나게 커다란 무언가가 끝도 없이 걷는 것이 이 세상의 이치다.** 그런데 요즘 그 속력이
느려졌다. 그것이 문제다. — **여기까지가 회원님이 정하신 것이다.**

#### ~~걷는 것이 무엇의 이치인지는 아무도 모른다~~ ⚠️ 폐기 (2026-08-23)

**★ 2026-08-23에 회원님이 정하셨다 — 걷는 것은 곧 낮과 밤이다.**
그것이 지나갈 때가 밤이고, 지나가고 나면 낮이다.
**우리가 자전을 당연하게 여기는 것처럼, 이 마을에서는 그것의 이동이 당연한 것이다.**
신비로운 것이 아니라 제일 뻔한 사실이라, 다들 안다.

그래서 그것이 느려지는 것은 **해가 느려지는 것**과 같은 일이 된다.

아래는 폐기된 옛 초안이다.

> ~~**"그냥 이치야."** 걸어서 무엇이 굴러가는지는 아무도 모른다. 옛날부터 걸었고 멈추면 안
> 된다는 것만 안다. 시간도 물도 경계도 아니다 — **밝히지 않는다.**~~

**이것이 결사와 같은 모양이다.**

> 그것도 이유를 모르고 걷고, 사람들도 이유를 모르고 따라 걷고, 결사도 왜 다니는지 안 밝힌다.

첫 꿈이 결사를 거울처럼 비춘다. 풀고 서고로 돌아왔을 때 *"나는 왜 이 일을 하지"*가 저절로
따라오고, 그것이 보류해 둔 **주인공의 목표**로 이어지는 길목이 된다.

**값을 치러야 한다 — 느려지는 것이 왜 나쁜지를 대사로 설명할 수 없다.** 눈으로만 보여준다.

- **부스러기.** 몸이 떨어져 나가는 것이 보인다. 무너지는 중이라는 것을 아무도 말 안 해도 안다
- **두고 간 짐.** 행렬이 줄고 있다. 포기한 자리가 길에 남아 있다
- **한 번도 없던 일이다.** 왜 나쁜지는 몰라도 전례가 없다는 것은 안다. 모르는 것이 공포가 된다
- **아직 아무 일도 안 일어났다.** 느려진 지 한참인데 하늘도 땅도 그대로다. 뭐가 올지 모르는
  채로 다들 기다리고 있다

#### ★★ 주인공은 이상이 생겼다는 것만 알고 온다 (회원님, 2026-08-21)

**와서 찾아야 한다.** 해법을 알고 오는 것이 아니다. 결사가 다닌다고 해서 답을 아는 것은 아니다.

**이것이 "이유가 없다"와 맞물려서 결말을 낸다.**

> 주인공은 **원인을 찾으러** 온다. → 그런데 **원인이 없다.**

찾아다니고 단서를 모으고 하나씩 지워나가다가 **아무것도 안 남는 것**이 이 단편의 답이다.
누가 잘못한 것도 무언가 들어온 것도 아니고 그냥 늙었다. 그것을 알아낸 순간 그것 앞에 선다.

**탐색이 진짜 탐색이 된다.** 정해진 일을 하러 가는 것이 아니라 뒤지고 다니는 것이고,
무엇을 발견하든 답이 아니다.

**"주인공은 다 알고 플레이어만 모른다"와 안 부딪힌다.** 주인공이 아는 것은 **세계관과 절차**고
이 꿈의 사정은 처음이다. **이 꿈에서만은 주인공과 플레이어가 같이 알아간다.**

#### ★ 단서를 뒤지는 것이 아니다 — 행렬을 만나는 것부터 (회원님, 2026-08-21)

단서 넷을 모아 지워나가는 안이 나왔는데 회원님이 잘랐다. **그것은 탐정물이고 이 게임은
순례다.** 도착하면 **먼저 행렬을 만난다.**

> **아무도 급해하지 않는다.**

그것이 느려졌는데 사람들은 그냥 걷는다. 늘 그랬다는 듯이. **급한 것은 주인공뿐이다.**
그 어긋남이 이 꿈의 첫 장면이고, 그러면 결사 쪽이 오히려 이상해 보인다 — 남의 세상에
뛰어들어와 혼자 안달하는 사람.

그리고 뒤져서 알아내는 것이 아니라 **같이 걸으면서 듣는다.** 탐색이 수색이 아니라 동행이 된다.
*"이유가 없다"*도 심문해서 캐내는 것보다 **그들이 이미 받아들이고 있는 것**으로 나오는 것이 세다.

#### ~~행렬은 같은 곳을 빙빙 돈다~~ ⚠️ 반은 정정됨 (2026-08-24)

**"돌아온다"는 맞는데 "돈다"는 틀렸다**(회원님, 2026-08-24). 원을 그리며 도는 것이 아니라
**이 세계의 공간이 이어져 있어서 곧게만 걸어도 제자리로 돌아온다.** 그래서 맵도 도넛이
아니라 **가로로 감긴 띠**다(`첫꿈.md` 5.4).

아래의 "한 바퀴가 곧 주기"라는 얘기는 그대로 유효하다 — 도는 방식만 바뀌었다.

**끝도 없이 걷는다 = 감긴 세계를 곧게 걷는다.** 세상이 무한하지 않은데 끝없이 걸으려면
어느 쪽으로든 돌아와야 한다.

**그러면 시계가 생긴다.** 한 바퀴가 이 세계의 주기고, **느려졌다는 것은 한 바퀴가 길어졌다**는
뜻이다. *이치를 안 밝히기로 했는데도* 느려진 것이 눈에 보이는 방법이 여기서 나온다 —
사람들이 "예전엔 이맘때 돌아왔는데" 하면 그걸로 끝난다.

**⚠️ 진짜 시계는 안 돈다** (회원님이 못 박으심). 실제 시간으로 재는 것이 아니라 **설정상
그때쯤 오는 것**이다.

**방아쇠는 마을 이벤트를 다 보는 것이고, "행렬이 돌아올 때가 됐다"는 그것을 설명하는 말이다.**

**서고에서 이미 돌아가는 것과 똑같다** — `Records.gd`가 *넷을 다 읽으면 → 북쪽이 열린다*를
한다(`all_read` → `open_way`). 여기서는 *마을 이벤트를 다 보면 → 그것이 온다*가 되고
**코드는 같은 것을 쓴다.**

#### 한 바퀴 — 이 꿈의 구조 (회원님)

```
도착  →  행렬을 만난다  →  "어디어디 마을로 가라"
      →  그 마을에서 실마리를 찾는다
      →  다 찾을 때쯤 행렬이 한 바퀴 돌아 돌아온다
      →  그것과 마주한다
```

**여기서 새로 생긴 것 — 안 걷는 사람들.**

행렬은 돌고 마을은 그 자리에 있다. 그러면 이 세계에 **걷는 사람과 안 걷는 사람**이 갈린다.
실마리가 마을에 있는 이유도 저절로 나온다 — **한자리에 있으니 쌓인다.** 지나가는 사람은
못 보는 것을 남아 있는 사람이 본다.

**마을이 그 세계의 작은 서고다.** 거점과 같은 모양이라 안 겉돈다.

#### 사람은 가까이 가면 말한다. 얼굴은 안 그린다 (회원님)

필드에서는 **작은 뒷모습·실루엣**이고, 다가가면 **대사창이 뜬다.** 초상화는 안 그린다.

- **이미 있는 대사 시스템을 그대로 쓴다.** 그림이 거의 안 늘어난다
- *"사람을 안 그린다"*는 결정과도 안 부딪힌다 — 등장인물 아트를 안 만드는 것이지 말을 못
  거는 것이 아니다
- **`Records.gd`가 그대로 재활용된다.** *다가가면 → 대사 → 다 하면 길이 열린다*가 서가에서
  행렬로 옮겨 붙는다. 구조가 똑같아 새로 만들 것이 없다
- 단, 서고와 똑같이 굴면 반복으로 느껴진다. 서고는 **긴 글을 읽는 것**, 여기는 **짧게 말을
  섞는 것**으로 갈라야 한다

#### ⚠️ 아래는 내가 얹은 것이고 아직 승인 안 났다

**갈라 적는다.** 한 번 뒤섞였다가 회원님이 잡아내셨다("그것과의 전투는 내가 안 짰잖아").
정한 사람이 누구인지 안 적으면 다음 세션에 내 추측이 설정으로 굳는다.

| 내가 얹은 것 | 왜 그렇게 생각했나 |
|---|---|
| ~~**한 걸음 = 하루**~~ | **폐기됨.** 이치는 안 밝히기로 정해졌다(위) |
| **자면서 걷고 깨면 멈춘다** | "느려짐 = 깨어나는 중"으로 이으려고 |
| **뒤에서 발소리를 내주면 다시 걷는다** | 그림의 행렬이 왜 따라가는지를 설명하려고 |
| **눈 여섯 감기기 전투** | 그림 위쪽의 은빛 눈들 + `EnemyDef.watches` 재활용 |
| **결사가 하는 일은 미루는 것** | "이유 없이 늙는다"에서 따라 나온다고 봤다 |

**전투는 아직 정하지 않는다**(회원님). 그리고 짤 때 **전투 = 공격이 아니다** — 이 게임은
`TalkOption`으로 **대화가 공격과 나란히** 있고, 서고의 그것은 아예 대화로만 풀린다.
그것과의 전투도 **한 대도 안 때리고 끝날 수 있다.**

#### ★★ 왜 느려졌나 — 이유가 없다. 늙는 중이다 (회원님)

넷 중 제일 어려운 것을 고르셨고, **이것이 게임 전체를 다시 정의한다.**

**이유가 없으면 적이 없다.** 잘못한 사람도 막는 자도 없다. 그리고 **고칠 수가 없다.**
그래서 주인공이 하는 일이 이렇게 된다.

> **결사가 하는 일은 고치는 것이 아니라 미루는 것이다.**

첫 꿈이 그것을 가르친다. 그리고 **결사의 목표는 여전히 안 밝혀진다** — 오히려 더 궁금해진다.
*못 고치는 것을 알면서 왜 계속 다니는가.*

#### 그럼 무엇과 싸우나 — 늙어서 떨어져 나온 것들

**늙으면 떨어진다.** 굳은 살·비늘·부스러기가 몸에서 떨어진다. **그런데 떨어진 조각은 아직
걷는다.** 본체는 느려지는데 부스러기는 여전히 부지런하다.

- **덤비지 않는다. 그냥 걷는다.** 부딪히면 다칠 뿐이다 — 지금 이 게임 적들이 다 그 성격이다
- 크기별로 몇 종이면 된다. **새 개념을 안 만든다**
- 죽은 것도 산 것도 아니고 **습관만 남은 것**이라 슬프고 무섭다

#### 마지막은 그것과 마주 선다 (회원님)

**부스러기는 필드 적이고, 끝에는 그것 앞에 서서 조우 연출을 거쳐 전투 장면으로 넘어간다.**
여기까지가 정해진 것이고, **그 전투가 무엇인지는 아직 안 정했다.**

어려운 지점은 이것이다 — *"이유가 없고 늙는 중이라 못 고친다"*를 정해 놓았는데, 그러면
때려서 될 일도 이겨서 될 일도 아니다. 나온 갈래는 이렇다(전부 내가 낸 것이다).

| | |
|---|---|
| A | **눈을 감긴다.** 눈 여럿이 각각 대상, 다 감기면 끝, 턴 갈수록 다시 뜬다. 지금 전투 구조를 그대로 쓰지만 **서고의 그것과 겹친다** |
| B | **걸음 자체가 전투.** 목표는 한 걸음 떼게 하는 것. 매 턴 발을 들었다 만다 |
| C | **몸 안으로 들어간다.** 늙어 막힌 것을 뚫는다. 전투 화면이 완전히 달라진다 |
| D | **가장 큰 부스러기와 싸운다.** 제일 전투답고 제일 싸다. 대신 그것 본인은 배경이 된다 |
| E | **그것의 꿈으로 들어간다.** 상대는 빠르게 걷던 시절의 자기 자신. 늙음과 싸운다는 것이 문자 그대로 된다 |
| F | **이기는 것이 없다. 버틴다.** 정해진 턴을 견디면 한 발을 뗀다. 제일 정직하고 제일 지루해질 위험이 크다 |

**★ 짤 때 잊지 말 것 (회원님):** **전투 = 공격이 아니다.** `TalkOption`으로 대화가 공격과
나란히 있고, 서고의 그것은 아예 대화로만 풀린다. **한 대도 안 때리고 끝나는 전투가 이미
이 게임에 있다.** 위 여섯 갈래 중 어느 것을 고르든 그 폭을 쓸 수 있다.

#### 위험 하나 — 허무해질 수 있다

못 고치는 것을 알고 하는 일이라 플레이어가 허무해질 수 있다. 막는 방법은 하나뿐이다 —
**보상을 눈에 보이게.** 하늘색이나 그림자처럼 **세상이 실제로 달라지는 것**이 보여야 한다.
짧아도 확실하면 허무하지 않고 애틋해진다.

#### 📄 구현 스펙은 `첫꿈.md`에 따로 있다

크게 짜 놓은 것만으로는 못 만들어서(회원님) **실제로 만들 수 있는 수준까지 내렸다.**
씬 구성·맵 모양·새로 쓸 스크립트·뽑을 그림·짓는 순서가 거기 있다.

**요점 둘만 여기 적는다.**

1. **새로 쓰는 스크립트는 둘뿐이다** — 행렬(`Caravan.gd`)과 부스러기(`Crumbs.gd`).
   나머지는 서고 장치의 껍데기만 바꾼 것이다. 특히 `Records.gd`의
   *다가가면 → 대사 → 다 하면 열린다*가 **마을 이벤트로 그대로 옮겨 붙는다.**
2. **★ 그림 없이 3단계까지 간다.** 걸어다니기 → 이야기 돌리기 → 전투까지 **비용 0**으로
   확인할 수 있다(타일셋과 적 그림은 서고 것을 임시로 쓴다). **재미없으면 한 푼도 쓰기 전에
   갈아엎는다.** 그림은 4단계에서 처음 뽑는다.

그리고 알아낸 것 하나 — **"거점은 좁고 어둡고 꿈은 넓고 밝다"가 코드가 아니라 씬 파라미터다.**
`Room.tscn`의 `DitherScreen`에 `light_radius`·`ambient`·`tint`가 있어서, 꿈 씬에서는 그 값만
올리면 된다. 한 줄도 안 고친다.

### 아직 안 정한 것

- **첫 꿈의 세부** — 사막 맵의 모양, 조우 연출을 어디서 트나, 눈이 몇 개인가
- **끝** — 꿈을 다 끝내면 무엇이 되나
- **그것** — 서고 안쪽의 문지기. 걷는 것의 눈 전투와 안 겹치게 벌려야 한다
- **그것** — 서고 안쪽의 문지기. 위가 정해져야 정해진다
- **주인공의 목표** (보류. 여정·결말이 모양을 갖춘 다음에 정한다)

### 지금 게임에 박힌 대사는 안 본다 (회원님)

스토리를 처음부터 짜는 것이라 **`resources/*.tres`의 적 대사들은 참고하지 않는다.** 그쪽도
나중에 이 톤에 맞춰 다시 쓴다. `watcher.tres`의 *"나는 결사의 수호자다"*는 **주인공 대사**다.

---


## 서고에서 만난 것과 그 자리에서 싸우다 (2026-08-18)

### 지금 어디까지 왔나

서고를 걷다 종이 더미를 밟으면 **그 자리에서 전투로 이어진다.** 그것의 15초짜리 조우
연출은 나선 한가운데의 그것에게만 남겼다.

```
1.1칸 안   드드드 떨림 1.0초 + 부스럭
탑이 선다  아홉 장이 쌓임 0.8초 + 뜸 0.25초
조리개     탑과 나만 남기고 0.9초에 걸쳐 닫힘
컷 암전    0.35초
복도       일러스트가 작게 떠서 카메라가 도는 1.5초 동안 제 크기까지 자람
전투
```

`Encounter`에 정적 변수 둘(`pending_enemy`·`straight`)을 뒀다. 씬을 갈아타면 값이 안
넘어가므로, 다른 화면이 "이 적으로, 곧장 전투부터"라고 적어 놓고 씬을 바꾼다.
**적을 하나 더 만들 때도 이 두 줄만 적으면 된다.**

### 적마다 다른 것 (`EnemyDef`)

| | 그것 | 종이로 된 것 |
|---|---|---|
| `battle_height` | 1.9 (화면 밖으로 넘침) | 0.796 |
| `battle_at` | 기본 (698, 166) | (636, 226) |
| `aura` | 켜짐(일렁임) | 꺼짐 |
| `breathes` | 켜짐(떠 있음) | 꺼짐(바닥에 섬) |
| `watches` | 켜짐(안 덤빔) | 꺼짐 |

**곡은 적이 안 든다.** 같은 적을 다른 데서 만나면 다른 곡이어야 하므로 **자리가 정한다** -
`Walker`가 서고 곡을 켜고, `BattleStage`는 곡을 아예 안 건드린다. `Music.keep()`이 곡을
트리 뿌리에 매달아 씬을 갈아타도 안 끊는다.

### 맞으면 맞은 티가 난다

- **그것/종이** — 낱장이 흩어지고(`EdgeBleed.hold`가 순간 안쪽까지 파고듦) 뒤로 밀리며 떤다
- **나** — 등불이 휘청이고 깜빡이며, **화면 전체가 흔들린다**(UI는 폭 45%)
- 셋 다 **깎인 체력에 비례**한다. 다 똑같이 튀면 한 대의 무게가 안 느껴진다

### 등불 밝기가 화면을 정한다

어둠 반경이 곧 밝기다(0.05~0.75). 곡선으로 눕히고 천장을 한 칸 내렸다 - 반경 1이면 이미
구석까지 닿아서 위쪽 두 칸이 붙어 있었고, 구석까지 훤하면 어둠 속을 걷는 이야기가 아니게
된다. 밝기에 바닥(0.14)을 깔아 한 칸 내릴 때 확 닫히지 않게 했다.

### ★★ 오늘 크게 깨진 것 셋

1. **Bash로 배경 실행한 창은 명령이 끝나면 같이 죽는다.** 회원님이 "달라진 게 없다"고
   여러 번 말씀하셨는데 옛 창을 보고 계셨던 것이다. **PowerShell `Start-Process`로 떼어
   놓고 띄우고, 창 개수를 세어 확인할 것.**
2. **셰이더에서 `COLOR`에 덮어쓰면 밝기(modulate)가 사라진다.** 정점 단계에서 varying으로
   받아 곱해야 한다. 조각 단계의 `COLOR`는 이미 텍스처 색이 곱해진 값이라 거기에 또 곱하면
   그림이 두 번 어두워진다.
3. **유니폼이 있는지 묻는 방법을 믿지 말 것.** `get_shader_parameter`는 한 번도 설정 안 한
   값에 `null`을 돌려주고, `shader.has_parameter`도 거짓이 나왔다. 없는 유니폼에 값을 넣는
   것은 아무 일도 안 일어나므로 **그냥 넣으면 된다.**

**그리고 제일 값어치 있는 것** — "안 보인다"는 코드를 읽어서 못 찾는다. 회원님이 세 번을
말씀하시는 동안 나는 짐작으로 원인을 골라 "이게 문제입니다"를 세 번 반복했다. 값을 1로
고정해 **한 장 찍으니** 셰이더가 아니라 부르는 쪽 문제라는 게 한 번에 갈렸다.
`-- --paper --shot --hit`이 그 도구다.

### 손잡이

```
-- --paper          종이 전투로 바로
-- --paper --tune   막대로 값 잡기, 마우스로 자리 끌기, F5로 tools/_tune.txt에 저장
-- --paper --shot   한 장 찍기.  --hit 을 더하면 맞는 순간을 찍는다
-- --battle         그것 전투로 바로
```

### 다음에 할 것

- **소리가 꺼져 있다**(`ScreenEffectLayer.MUTED = true`)
- `ANIMATION.md` — 픽셀아트 애니메이션 만드는 법을 찾아 정리(`SOUND.md`와 같은 형식으로)
- 그것 조우에서 서고 곡을 낮추고(`Music.duck()`) 그것 곡 얹기
- 종이 만날 때 0.5초 안에 벗어나면 풀리는 것, 주인공이 멈추고 바라보는 것
- 전투에서 **지는 방법** - 그것이 안 때리므로 위험은 기름뿐인데, 기름이 다하면 무슨 일이
  벌어지는지가 없다

---
## 서고를 나선으로 만들고, 관문에서 이었다 (2026-08-14~15, 다른 컴퓨터)

**이 항목은 그 세션이 아니라 다른 컴퓨터에서 나중에 코드와 커밋을 읽고 적은 것이다.**
"왜 그렇게 정했나"는 전부 `TilesetRoom.gd`·`Sleepers.gd`·`Walker.gd`의 주석에서 가져왔다 —
그쪽이 원문이니 자세한 것은 거기를 볼 것.

### 서고는 나선이다

사각형 방 하나였던 것을 **심연 위에 놓인 고리 셋**으로 바꿨다. 회원님이 주신 참고 그림
셋(고리 세계 · 나선 코덱스 · 떠 있는 서가 탑)을 하나로 묶은 모양이다.

```
바깥 고리(19칸)   최근에 다녀온 꿈. 내가 남긴 기록
가운데 고리(14칸) 
안쪽 고리(9칸)    더 오래된 것. 누가 썼는지 모르는 것
중심(4.5칸)       그것이 있다. 잡으면 포탈이 열리고 그 너머가 마을이다
```

- 방은 44x44칸, 통로 반너비 1.6칸이다. **등불 지름보다 좁아야 벽이 보인다.**
- **고리 사이는 한 군데씩만 뚫려 있고 그 자리가 고리마다 돌아간다.** 곧장 못 들어가고 돌아
  걸어야 한다 — 등불이 좁아 한 번에 조금밖에 안 보이는 것과 맞물려 서고가 넓게 느껴진다.
- **다만 마지막 한 걸음만은 남쪽에 못 박았다.** 거기로 들어가면 반드시 북쪽을 향해 걷게 되고,
  그래야 조우 화면(뒷모습으로 북쪽을 향해 걸어 들어감)과 방향이 이어진다.
- **중심은 마을이 아니다**(08-15 정정). 마주 서는 자리일 뿐이라 넓을 이유가 없고, 오히려
  좁아야 도망갈 데가 없어 보인다. 마을은 포탈 너머의 **계단의 방**이다.

타일셋도 새로 뽑았다. `marble_void`(흰파랑 대리석 + 남색 공허) → `wood_chasm`(따뜻한 나무 +
심연). 참고 그림의 호박색과 안 맞아서다. **옛 시트가 남아 있어 두 줄만 되돌리면 그대로 쓴다.**

### 중심으로 가는 길은 잠겨 있다

**서가 넷을 다 읽어야 마지막 통로가 열린다.** 그래야 기록이 장식이 아니라 관문이 된다.
읽으려면 등불이 밝아야 하고 등불은 기름을 태우므로, **알아내는 데 값이 드는 구조**가 여기서
처음으로 실제 규칙이 됐다.

서가는 **통로가 뚫린 자리에서 멀찍이** 둔다. 지나는 길에 저절로 밟히면 찾은 것이 아니라
걸린 것이다. 처음엔 둘이 입구 근처에 몰려 있어서 시작하자마자 밟혔다(08-15).

### 종이로 된 것

바닥에 누워 있다가 다가가면 일어선다. **무서운 이유는 구별이 안 되기 때문이다** — 서고에는
원래 종이가 사방에 흩날리고 있어서(`Pages`), 지나가려던 쓰레기가 일어서는 것이 이 적이다.

- 반응 거리 1.1칸은 **등불 반경보다 작다.** 먼저 부스럭거리고, 거기서 물러나면 안 깨운다 —
  보고 나서 피할 수 있어야 한다
- 일어서는 것은 **화면에 안 그린다.** 부스럭거리다 바로 암전, 눈을 뜨면 전투 화면이다.
  안 보여주는 쪽이 무섭고, 64px로 일어서는 그림은 어차피 잘 안 읽힌다

**★ PixelLab 교훈** — "일어서라"고만 하면 제자리에서 부스럭거리기만 했다. **끝 프레임
(`paper_stood.png`)을 못 박아 주니** 거기까지 갔다. 시작 자세와 목표가 멀면 모델이 안전한
쪽으로 도망간다.

### 판정을 한 군데로

**방의 모양이 `TilesetRoom._walkable()` 하나에 모여 있다.** 거기만 고치면 타일도 이동 판정도
지도 도구도 한꺼번에 따라온다. `Hero`는 방이 어떻게 생겼는지 모르고 판정을 밖에서 받는다.
판정을 두 군데 적으면 반드시 어긋난다.

### 위에서 내려다보는 지도

`tools/_map.gd`가 서고 구조를 그림과 글자로 뽑는다. **게임 화면은 등불이 좁아 통로가 제대로
놓였는지 알 수 없어서**, 위에서 봐야 안다. `seogo_map.png`를 저장소에 올려 두고 밖에서 폰으로
확인한다.

### 관문 → 서고

`ScreenEffect`가 오토로드라 **씬을 갈아타도 암전이 그대로 남는다.** 넘어온 쪽(`Walker`)에서
1.6초에 걸쳐 걷어낸다. 서고를 따로 열었을 때는 이미 투명해서 아무 일도 안 일어난다.

---

## 전투를 다시 지었다 — 소리도 (2026-08-14)

### 지금 어디까지 왔나

조우 복도부터 전투까지 한 줄로 이어진다. **씬을 안 갈아탄다** — 조우 화면 위에
`BattleStage`가 얹히고 그것과 내가 좌우로 옮겨 앉는다(카메라가 도는 것). 복도도 등불도
그것도 한 번도 안 사라진다.

```
걷기 8초 → 발견(멎음) 1.0 → 그그그긍 44칸 2.6 → 쳐다봄 1.3 → 울음 2.6 → 정적 0.9
→ 다가옴(공포 소리·화면 떨림, 가까울수록 셈) → 팟, 등불이 꺼짐
→ 우웅…… 우웅…… → 팟팟팟 빛살 14 → 흰 화면 0.45 → 카메라 부우웅 1.5 → 전투
```

`res://scenes/Encounter.tscn -- --battle`로 켜면 이 25초를 건너뛰고 전투로 바로 간다.

### 전투를 셋으로 갈랐다

| 파일 | 맡는 일 |
|---|---|
| `Battle.gd` | 규칙. **화면을 전혀 모른다.** 적을 목록으로 들어서 여럿과 싸울 수 있다 |
| `BattleStage.gd` | 무대. 아무 씬에나 얹힌다. 카메라를 돌리고 규칙과 글자를 잇는다 |
| `BattleHud.gd` | 글자와 메뉴. 배치도 연출도 모른다 |
| `BattleScreen.gd` | 전투만 따로 켜 보는 시험대(232줄 → 76줄) |

- **숫자를 안 보여준다.** 체력·적 상태 글자를 다 지웠다. 남은 것은 등불 밑의 기름 눈금
  하나이고, **그 눈금의 색이 곧 체력**이다(다칠수록 붉어진다).
- **한 턴을 벌려 놨다.** 규칙은 한 번에 끝나지만 화면이 받아 뒀다가 한 줄씩 푼다
  (`_beats`). 푸는 동안 버튼이 안 먹고, 맞을 때 세상이 한 번 앓는다.
- 메뉴: 공격·기술·대화·방어가 부챗살, **도주만 아래로 따로** 뻗는다. 공격과 대화는
  대상을 묻고, 대상·말길에는 "그만두기"가 붙는다.
- **말길을 적이 들고 있다**(`TalkOption`). 규칙은 벌어질 수 있는 일 다섯 가지만 알고
  (NOTHING·OPENS·FLINCH·ANGER·READS) 무엇이 뜰지는 리소스가 정한다.

### 그것

**덤비지 않는다**(`EnemyDef.watches`). 제 차례에 쳐다보기만 하고, "고리가 한 칸 돌아간다"
줄에서는 실제로 한 칸 돈다(글 앞의 `[돈다]` 표). 위협은 안 통한다 — *"그것은 그저 본다.
위협이 닿을 자리가 없다."* 이름은 **"그것"**.

### 소리를 다 코드로 찍었다

`tools/_sfx.gd` → `assets/sfx/*.tres` 열여섯 개. **`.tres`로 저장한다** — `.wav`로 저장하면
`ResourceSaver`가 그 확장자를 못 다뤄서 **아무것도 안 쓰고 조용히 실패한다**(파일이 하나도
없는데 "저장"이라고 찍혔다). 저장 실패를 이제 오류로 띄운다.

**★ 배운 것 셋**

1. **사인파를 쓰면 무조건 금속으로 들린다.** 발소리를 34Hz 사인으로 만들었더니 대포,
   205Hz 사인으로 만들었더니 핀셋, 빛살에 1900Hz를 얹었더니 종이 됐다. 재료가 있는 소리는
   **잡음을 걸러서** 만든다(공진 필터).
2. **좁게 거르면 신시사이저, 넓게 거르면 파도.** 바람이 그 사이에 있다.
3. **"안 들린다"를 파형 탓으로 넘기지 말 것.** 낮아서 안 들린다고 했다가 재보니
   울음이 열여섯 중 제일 큰 파일이었다(`tools/_probe_sfx.gd`가 세기를 재 준다).
   진짜 원인은 같은 순간에 겹친 다른 소리였다.

복도 울림은 소리마다 넣지 않고 **`_save`가 나가는 길목에서 한 번에** 씌운다(0.075/0.165/
0.290/0.430초 뒤 30·20·13·7%). 도는 소리(바람·공포)는 이음매가 망가지므로 뺀다.

### 음악

`assets/music/title.mp3`(26초부터), `battle.mp3`(2초부터). `Music.play_in()` 한 줄로 켠다 —
mp3는 임포트에 루프가 꺼진 채 들어와서 불러온 자리에서 켜 준다.

### 다음에 할 것

- **등불이 꺼지는 소리를 유리로.** 마주친 순간 나는 "팟"을 **쩌적쩌적쩌적 쩅그랑**으로
  바꾼다 — 등불은 놋쇠 통에 유리가 끼워진 물건이라 그쪽이 맞다. 만드는 법은
  `SOUND.md`에 적어 뒀다.
- **기술** 메뉴가 비어 있다. 지금은 "아직 익힌 기술이 없다"만 뜨고 턴을 안 쓴다.
- 이 전투를 **어떻게 지는지** 안 정했다. 그것이 안 때리므로 위험은 기름뿐인데,
  기름이 다하면 무슨 일이 벌어지는지가 없다.
- 적이 여럿일 때 **그림을 여러 개 세우는 것**(무대가 아직 스프라이트 하나만 안다).
- `resources/hollow_armour.tres`는 옛 형식이라 말길이 비어 있다.
- 대상 고르다 물리기를 "그만두기"로만 한다(Esc는 게임 종료라 다른 키가 필요).

---

## 🕯️ 화풍을 정했다 (2026-08-11)

첫 세션. 발할라·커피톡 같은 **대화 기반 게임**을 만들기로 했고, 참고로 가져온 그림은 고딕·오컬트
분위기의 1비트 흑백 디더링 픽셀아트 넉 장이었다. 이번 세션에서 한 것은 **그 화풍을 실제로 뽑는
방법을 확정한 것**이고, 게임 자체는 아직 한 줄도 없다.

### 회원님이 정한 것

| | |
|---|---|
| 만들 곳 | 습작4 (우주쓰레기게임과 별개의 새 게임) |
| 화풍 | 1비트 흑백 디더링 — 단 완전 흑백이 아니라 **"색이 있는데 흑백 필터를 낀 느낌"** |
| 남길 색 | 그 그림에 필수인 색만. 피면 빨강, 숲이면 초록, 스테인드글라스면 여러 색 |
| 구조 | 거점 하나 + 장소 몇 곳 (아직 확정 아님) |
| 컨셉 | **주인공이 등불을 들고 다닌다** |

### 화풍을 만드는 방법

AI에게 "1비트로 그려줘"라고 시키지 않는다. **컬러로 뽑고 우리 필터를 씌운다.**

```
PixelLab로 컬러 배경을 뽑는다  →  실행 중에 DitherFilter를 씌운다  →  등불만 필터 밖에 얹는다
```

같은 필터를 통과시키면 배경이 몇 장이든 화풍이 저절로 통일된다. 그리고 **필터는 PNG를 바꾸지
않으므로** 나중에 값 하나만 고치면 전부가 따라온다. 이게 이 방식을 고른 이유다.

색은 **원본의 채도로 자동 판별**한다. "중요한 색"을 기계가 알 수는 없지만, 스테인드글라스·불·
이끼처럼 꼭 있어야 할 색은 원본에서도 대개 채도가 높다는 전제다. 예배당에서 실제로 잘 맞았다.

### 만든 것

| 파일 | 하는 일 |
|---|---|
| `shaders/DitherFilter.gdshader` | 계조 깎기 → 8x8 오더드 디더 → 틴트 → 채도 높은 곳만 색 복원 |
| `tools/FilterPreview.gd` / `.tscn` | 슬라이더로 그 값을 맞추는 도구. Space 배경 넘김 / L 등불 / Tab 패널 / C 값 복사 / S 저장 |
| `assets/backgrounds/chapel.png` | 버려진 고딕 예배당. pixen 640x360, 1 generation |
| `assets/backgrounds/forest.png` | 어두운 숲. 색이 많은 장면에서도 같은 값이 먹히는지 시험용 |
| `project.godot` | 640x360 내부 해상도, 정수배 확대, nearest 필터 |

확정된 값은 `CLAUDE.md`에 적어뒀다. D3D12에서 실제로 그려지는 것까지 확인했다.

### 겪은 것 두 가지

- **숲에서 초록이 거의 죽었다.** 나무의 초록이 원래 탁해서 채도 기준을 못 넘고, 살아남은
  색마저 탈색량 0.5에 절반이 된다. 회원님은 **그대로 좋다**고 하셨다 — "색이 남은 곳이 곧
  시선이 가는 곳"이라는 규칙으로 받아들이기로 했다
- **배경 안의 촛불도 회색이 된다**는 문제를 제기했으나(등불만 색이라는 규칙과 충돌),
  회원님이 **접자고 하셨다.** 배경은 다시 뽑지 않는다. 등불은 그냥 주인공이 드는 물건이다

### 시행착오 (다시 겪지 말 것)

- 처음에 "black and white monochrome"으로 프롬프트를 짰다가 뒤집었다. **원본에 색이 없으면
  필터가 걷어낼 색도 없다.** 컬러로 뽑아야 한다
- 탈색량을 1.0으로 올려 세상을 완전 회색으로 만들어봤으나 **0.5로 원복**했다

---

---

## 💬 대화 시스템을 옮겨왔다 (2026-08-11, 같은 세션)

우주쓰레기게임(`Documents/연습/우주쓰레기게임`)에 이미 있던 것을 골라 왔다. **복사해오고 두
프로젝트가 갈라지는 것을 받아들이기로 했다** — 공유 라이브러리로 묶으면 컴퓨터 두 대를 오가는
상황에서 폴더가 하나 더 늘어 오히려 짐이 된다.

### 가져온 것

| | |
|---|---|
| `scripts/dialogue/` | Controller(354줄) + 데이터 스키마 6개. **거의 그대로** |
| `scripts/dialogue/commands/` | wait, fade_in/out, flash, blackout, flicker, shake, set_auto_advance |
| `scripts/ScreenEffectLayer.gd` | 페이드/암전/플래시. 자체 완결이라 그대로 |

Controller의 진짜 값어치는 코드가 아니라 **주석에 남은 버그 기록**이다 — 재진입 방지가 왜 bool이
아니라 카운터인지, 짧은 줄이 빠른 두 번 클릭에 씹히던 것, 스킵이 첫 줄만 넘기고 멈추던 것,
연출이 긴 줄이 연출 끝나기 전에 넘어가던 것. 새로 짰으면 넷 다 다시 겪었다.

### 안 가져온 것 (필요해지면 그쪽에서 다시 꺼내면 된다)

- `zoom` — GameCamera가 있어야 한다. 이 게임엔 카메라가 없다
- 사운드 커맨드 4개 — `SfxManager`(448줄, 그 게임 효과음 파일이 박혀 있다)가 통째로 딸려온다
- `emergency_light` / `cargo_panel` — 우주선 비상등, 화물칸
- `DialogueUI` — **구조만 베끼고 새로 짰다.** 그쪽은 SF HUD 테두리와 CRT 켜짐 연출이 붙어 있어
  이 게임의 회색 고딕 화면과 어긋난다
- `DialogueEffectLayer`(CRT 스캔라인), `DialogueOpening`(512줄), `DialogueShowcase`(316줄)

### 새로 만든 것

| 파일 | 하는 일 |
|---|---|
| `scripts/DialogueUI.gd` | 대사창. 시그널만 구독하는 얕은 결합은 그대로, 겉모습은 새로 |
| `scripts/FilteredBackground.gd` | 배경에 DitherFilter를 씌워 띄운다. 배경은 이 노드로만 올린다 |
| `scripts/LampGlow.gd` | 등불 빛. **필터 바깥**에 있어야 색을 지킨다(자식으로 넣으면 회색이 된다) |
| `scripts/KoreanFont.gd` | Godot 기본 폰트에 한글이 없어서 시스템 폰트를 빌린다 |
| `scenes/DialogueTest.tscn` | 확인용. 타이핑/진행/style/커맨드/선택지가 도는지만 본다 |

메인 씬은 `scenes/DialogueTest.tscn`이다. 실제로 D3D12에서 돌려 화면까지 확인했다.

### 알아둘 것

- 두 프로젝트의 내부 해상도가 **똑같이 640x360**이라 레이아웃이 그대로 맞았다
- 다만 확대 방식이 다르다. 그쪽은 `canvas_items`(글자만 고해상도로 또렷), 이쪽은
  `viewport`(전부 640x360에서 그린 뒤 확대). 이쪽이 픽셀아트엔 맞지만 **글자가 도트로 뭉툭해진다**
- **한글 픽셀 폰트가 없다.** 지금은 맑은 고딕(안티앨리어싱된 벡터 폰트)을 빌려 쓰는 임시 상태라
  글자만 화풍에서 겉돈다. `KoreanFont.gd`만 갈아끼우면 되게 해뒀다

---

## 다음에 할 일

### 🗡️ 게임의 방향이 정해졌다 (2026-08-11)

처음엔 발할라·커피톡 같은 **대화 기반**으로 잡았는데, 이야기하는 과정에서 회원님이 방향을
바꾸셨다. 아래가 회원님이 직접 정하신 것이다.

| | |
|---|---|
| 톤 | **다크 판타지** |
| 주인공 | **판타지 세계를 떠도는 방랑 모험가** — 가게 주인이 아니라 길 위의 사람 |
| 전투 | **있다. 피어 앤 헝거(Fear & Hunger) 같은 느낌** |

**"손님이 나에게 온다"가 "내가 찾아간다"로 뒤집혔다.** 그래서 등불이 오히려 자연스러워졌다 —
가게 주인이 등불을 드는 건 어색했지만 밤길을 걷는 방랑자가 드는 건 당연하다.

**아직 안 정한 것: 세계관, 왜 떠도는가, 전투 세부.** 그리고 **게임의 무게중심이 전투인지 대화인지**도
안 정했다 — 이건 말로 정하지 말고 전투 한 판 만들어 돌려보고 정하기로 했다.

이 결정의 대가를 적어둔다. 피어 앤 헝거는 대화 게임이 아니라 던전 생존 게임이다. 그 전투를
가져오면 **지금까지 만든 대사 시스템은 주인공이 아니라 조연이 된다.** 알고 가는 것이다.

### 🌌 세계관이 정해졌다 (2026-08-11) — 회원님이 정하신 것

```
이 세상들은 어떤 초월적인 무언가의 꿈이 세상이 된 것이다.
그 꿈들에 이상이 생긴다.
주인공은 그 꿈으로 건너가 그곳의 문제를 푼다.
```

| | |
|---|---|
| 주인공 | **세계를 지키는 비밀 결사의 수호자** |
| 거점 | **아카식 레코드처럼 보이는 곳** — 모든 것이 기록된 서고 |
| 결사의 목표 | **맥거핀. 밝히지 않는다** |
| 장소마다 노릴 것 | **순례 · 경외 · 공포 · 신비로움** |

**결사의 목표를 안 밝히는 것이 방침이다.** 이 게임은 이기는 게 아니라 세계관을 즐기는
게임이라, 답을 주면 오히려 재미가 준다.

### 게임 시작에 띄울 문장

> **세계를 건너다니는 자에게는 그곳이 온전히 보이지 않는다.**
> **등불이 비추는 것만 실재한다.**

회원님이 고르신 문장이다. 이게 규칙 셋을 한꺼번에 설명한다 — 화면이 왜 회색인지, 색을 가진
것이 왜 등불뿐인지, 왜 기름을 태워야 볼 수 있는지. **설정과 시스템이 같은 문장으로 묶인다.**

### 이 세계관이 잠근 것들

- 참고그림 열두 장이 화풍도 색도 제각각인 것 → **꿈마다 다른 세계니까 당연하다.** 밝은 SF도
  어두운 고딕도 전부 쓸 수 있다
- 사진 더미가 왜 있는가 → **꿈은 잊히니까 기록한다.** 그리고 거점이 아카식 레코드(기록의 서고)
  라서, 사진 더미가 UI가 아니라 **결사의 물건**이 된다
- 필터가 왜 모든 세계를 회색으로 만드는가 → 위 문장 그대로. 기술적 편의가 아니라 설정이다

### 아직 빈 곳

- 결사는 **무엇으로부터** 세계를 지키나 (꿈의 "이상"이 무엇인지)
- 등불은 무엇인가 — 결사의 물건인지, 세계를 건너는 수단인지
- 왜 수호자는 혼자인가 (사람을 안 그리기로 했으므로 다른 단원이 화면에 안 나온다)

셋 다 첫 장소의 글을 쓸 때 필요해진다. 그전까지는 미뤄도 된다.

### 🖼️ 방향을 다시 잡았다 (2026-08-11, 세션 끝 무렵)

**"아트를 즐기는 게임."** 옛날 포인트 앤 클릭 어드벤처 느낌으로 간다. 회원님이 정하셨다.

```
사진 더미   여정을 보여주는 화면. 지나온 곳이 좌우로 쌓인다
장소 하나   사진 한 장. 들어가면 화면을 가득 채운다
그 안에서   둘러보고, 클릭하고, 읽는다  ← 게임의 대부분
가끔        조우가 벌어진다 (만들어둔 전투 화면)
```

**이기는 게 아니라 보는 게 즐거움이다.** 그래서 전투는 주인공에서 내려오고, **대사 시스템이
다시 주인공 자리로 돌아온다** — 클릭해서 뜨는 게 결국 글이니까. 앞서 "피어 앤 헝거로 가면
대사 시스템이 조연이 된다"고 적어둔 걱정이 뒤집혔다.

만들 게 적은 것이 이 방향의 진짜 이점이다. **장소 하나 = 사진 한 장 + 클릭할 자리 몇 개 +
각각에 딸린 글.** 배경 1 generation, 글은 공짜다.

**아직 안 정한 것: 등불을 커서로 쓸 것인가.** 화면이 캄캄하고 마우스가 닿은 곳만 빛이 들어
보이는 방식이다. 그림을 한 번에 다 못 보게 만들어 천천히 보게 하는 장치이고, 지금까지 세운
규칙 셋(*안 보이는 것이 무섭다* / *색을 가진 건 등불뿐* / *기름을 태워야 알 수 있다*)이 전부
같은 동작으로 모인다. 다만 제안 단계다.

### 🗂️ 사진 더미 (2026-08-11)

`scripts/PhotoStack.gd`, 확인용 씬은 `scenes/PhotoTest.tscn`(Space로 한 장씩 얹힌다).

**pixen이 가로로 긴 캔버스에서 그림을 좌우로 갈라 내놓는 것이 계속 결함이었는데, 사진이
정사각형이면 그게 형식이 된다.** 게다가 **갈라진 이음매에서 자르면 배경 한 장이 사진 두 장이
된다** — 회원님 아이디어다. 가운데를 자르는 게 아니라 대칭인 자리를 자른다.

배경 4장을 잘라 사진 8장이 나왔다(`assets/photos/`, `tools/_split.gd`). **generation 0.**
그리고 자른 것이 원본보다 낫다 — `tower_gate_2`는 탑 하나에 붉은 문, 위가 잘린 구도인데,
프롬프트로 아무리 요청해도 안 되던 그림이 자르니까 그냥 나왔다.

사진 한 장은 **320x320**이다. 앞으로 새로 뽑는 것도 이 크기로 맞춘다.

**회전은 쓰지 않는다.** 폴라로이드처럼 비스듬히 겹치면 보기 좋겠지만 픽셀아트를 각도로 돌리면
도트 격자가 깨진다. 평행 이동 + 테두리 한 줄 + 밝기 차로만 층을 나눈다.

### 떠오른 안 (확정 아님)

**"수많은 초월적인 곳을 다니는 누군가"** — 회원님이 던지신 것이고, 지금까지 어긋나던 게 여기서
한꺼번에 맞는다.

- 참고그림 열두 장이 화풍도 색도 제각각인 게 **문제가 아니라 답이 된다.** 열두 장소인 셈
- **필터가 그것들을 묶는 장치가 된다.** 서로 상관없는 세계들이 한 게임이 되는 이유가
  "전부 같은 회색을 통과한다"는 것. 화풍 통일이 기술적 편의가 아니라 **설정**이 된다
- **등불이 유일한 상수다.** 장소는 매번 바뀌는데 내가 든 불빛만 안 바뀐다
- 장소마다 필터 틴트를 다르게 줘도 된다 — 세계가 다르니 갈라지는 게 맞다
- 만들기도 좋다. **장소 하나 = 배경 하나 + 조우 하나**, 서로 안 얽혀서 하나씩 늘리면 된다.
  다섯 곳이면 게임, 스무 곳이면 큰 게임. 중간에 멈춰도 완성품이다

### 만들 것

**여정의 고리에서 새로 만들 건 둘뿐이다.** 장소 이동은 `DialogueChoice.next_scene`이 이미 하고
있어서 지도나 이동 시스템이 필요 없다 — **선택지가 곧 길이다.**

1. 배경을 바꾸는 대사 커맨드 (열 줄쯤)
2. **전투 화면** — 1인칭 턴제, 적 그림 한 장 + 아래 메뉴. 부위별로 노린다

적 그림은 **전투 설계를 확정한 뒤에** 뽑기로 했다(회원님 요청). 아직 안 뽑았다.

**초상화.** 우주쓰레기게임의 `DialoguePortrait`(184x400 흉상) 구조는 쓸 만하다. 다만 이 게임의
인물 크기를 먼저 정해야 하고, 그건 "누가 나오는가"를 알아야 정할 수 있다.

---

## 🔤 글꼴을 정했다 (2026-08-11, 같은 세션)

**Neo둥근모 16px.** 갈무리11(12px)·갈무리9(10px)와 같은 화면에 넣어 비교했고, 회원님이 둥근모를
고르셨다. 셋 다 SIL Open Font License 1.1이라 상업적 이용·게임 임베드에 문제가 없다.

`assets/fonts/`에 `Galmuri11.ttf`(5.4MB)와 `Galmuri9.ttf`(4.6MB)가 아직 남아 있다 — 안 쓸 거면
지워도 된다.

### 겪은 것 (다시 겪지 말 것)

- **갈무리 비트맵판은 함정이다.** `Galmuri11Bitmap.ttf`는 내장 비트맵이 16px에만, `9Bitmap`은
  12px에만 들어 있다. 다른 크기에서는 글리프 높이가 0이라 **글자가 통째로 안 그려진다.**
  파일이 1/7로 작아서 솔깃하지만 쓰지 말 것. 외곽선판은 어느 크기에서도 정상이다
- `disable_embedded_bitmaps`를 굳이 끄지 말 것. 기본값(끄지 않음 = 외곽선 사용)이 맞다
- **기울임(`[i]`)을 픽셀 글꼴에 걸면 안 된다.** 도트를 비스듬히 밀어서 격자가 깨진다.
  내레이션 스타일에서 뺐고, 지금은 색만 다르게 간다
- 임포트 설정은 **글꼴 파일마다** 꺼야 한다. project.godot의 `[gui]`는 기본 글꼴에만 걸린다

(선택지 버튼 스타일은 같은 세션에 끝냈다 — 대사창과 같은 톤으로 가라앉혀두고, 지금 고르고 있는
것만 테두리와 글자가 등불색으로 살아난다. 첫 버튼에 포커스를 줘서 위/아래 + 스페이스로도 고를 수
있다.)

아직 안 정한 것: **왜 이 세계가 이런가, 주인공은 누구인가, 찾아오는 이들은 왜 오는가.**
이건 회원님이 정할 몫이고, 정해져야 손님도 대사도 나온다. 컨셉으로 정해진 것은
**"주인공이 등불을 들고 다닌다"** 하나뿐이다.

---

## ⚔️ 전투를 만들었다 (2026-08-11, 같은 세션)

**처음으로 눌러볼 수 있는 것이 생겼다.** 메인 씬이 `scenes/Battle.tscn`이다.

| 파일 | 하는 일 |
|---|---|
| `scripts/battle/Battle.gd` | 전투 진행. **화면을 모른다** — 시그널만 쏜다 |
| `scripts/battle/Lantern.gd` | 등불. 밝기 3단계의 수치표가 전부 여기 있다 |
| `scripts/battle/EnemyDef.gd` | 적 정의(Resource). **새 적 = 리소스 하나**, 코드 무수정 |
| `scripts/battle/BattleScreen.gd` | 화면. 밝기를 셰이더로 흘려보낸다 |
| `resources/hollow_armour.tres` | 속 빈 갑옷 |
| `scripts/UiStyle.gd` | 대사창과 전투 화면이 같은 톤을 쓰게 모아둔 것 |

로직과 화면을 나눈 것은 대사 시스템(`DialogueController` / `DialogueUI`)과 같은 방식이다.

**말을 걸어 푸는 길에는 기름값이 든다.** 속 빈 갑옷은 말을 세 번 건 뒤 **등불이 환할 때만**
물러간다. 어두우면 "아무것도 보이지 않는 듯하다"가 뜨고 안 통한다. 설득이 공짜가 아니게
만드는 장치다.

### 알아낸 것

- **탈색만으로는 어림도 없다.** 처음엔 밝기 단계에 `desaturate`만 물렸는데, 색만 빠지고 화면이
  안 어두워져서 불을 꺼도 적이 또렷했다. `layer_modulate`로 **실제 밝기까지 같이 내려야**
  세 단계가 갈린다(`Lantern.BRIGHTNESS`)
- 셰이더가 `COLOR`를 통째로 덮어써서 **노드의 `modulate`가 조용히 무시된다.** 아무 일도
  안 일어나서 알아채기 어렵다. `layer_modulate` uniform을 쓸 것
- 적 체력은 **숫자로 안 보여준다.** "멀쩡하다 / 금이 갔다 / 무너지기 직전이다"로만.
  눈금이 보이면 계산이 되고, 계산이 되면 무섭지 않다

### 등불은 5단계다

꺼짐 / 불씨 / 어스름 / 밝음 / 환함. 수치표는 전부 `Lantern.gd` 맨 위에 모여 있다.

**세상과 불빛이 반대로 움직인다**(회원님 요청). 단계가 내려갈수록 세상은 어두워지고 색이
빠지는데(`BRIGHTNESS`, `DESATURATE`), **불빛 자체는 더 도드라진다**(`LIGHT_INTENSITY`).
캄캄할수록 작은 불빛이 강하게 보이는 게 실제로 맞고, 화면에서도 그래야 "이 불이 내가 가진
전부"로 읽힌다. **맨 아래 단계는 밝기 0 — 진짜 암흑이라 배경도 적도 사라지고 UI만 남는다.**

돌려보니 **어두운 쪽이 훨씬 낫다.** 불씨 단계에서 세상이 거의 죽고 빛줄기만 살아남는 화면이
이 게임의 그림이다.

### 메뉴가 등불에서 나온 빛이다

`scripts/battle/LightMenu.gd`. 상자 버튼을 늘어놓지 않고, 화면 아래 가운데(내가 등불을 든
자리)에서 빛줄기가 뻗어 나가고 그 끝에 **공격 / 방어 / 대화 / 도주**가 붙는다. 회원님이
스케치로 주신 구조다.

상자를 안 쓰는 이유는 이 게임 규칙 때문이다 — 화면에서 색을 가진 것은 등불뿐이라 UI에 회색
상자를 늘어놓으면 규칙이 깨진다. **메뉴가 빛이면 UI가 설정의 일부가 되고**, 등불이 약해질 때
메뉴도 같이 흐려진다. 단 글자만은 최소 밝기를 지킨다(`MIN_TEXT_ALPHA`) — 안 보여야 하는 것은
**적**이지 내 손이 아니다.

### CRT는 아직 확정 아니다

`shaders/CrtScreen.gdshader` + `scripts/CrtOverlay.gd`. 볼록렌즈 왜곡 + 가로 주사선 +
가장자리 어둡게. **C 키로 껐다 켰다** 하며 비교하라고 만들어둔 것이고, 쓸지 말지는 안 정했다.

대가가 하나 있다. 이 게임은 640x360을 정수배로만 확대해 도트가 화면 픽셀에 딱 떨어지는 것이
규칙인데 **왜곡이 그 규칙을 깬다.** 가장자리로 갈수록 도트가 늘어진다. 진짜 CRT가 그랬다고
볼 수도, 애써 지킨 격자를 스스로 무너뜨리는 것이라고 볼 수도 있다.

**함정 하나**: CRT가 켜진 채로 `get_viewport().get_texture().get_image()`를 부르면 힙 손상으로
즉시 죽는다. 화면 텍스처를 다시 읽는 셰이더와 스크린샷이 부딪힌다. 프레임을 두어 번 더
기다리면 피해진다(`BattleScreen._shoot`).

### 아직 안 해본 것

**밸런스를 한 번도 안 돌려봤다.** 수치는 전부 첫 짐작이다 — 특히 큰 공격 15가 체력 40에서
충분히 아픈지가 관건이다. 안 아프면 어둠 속에서 뭉개는 게 정답이 되고, 그러면 등불 선택이
성립하지 않는다. 수치는 전부 `Lantern.gd`와 `Battle.gd` 맨 위에 모여 있다.

---

---

## 📐 해상도를 960x540으로 옮겼다 (2026-08-11)

**640x360에서 올렸다.** 이유는 하나다 — **512x512 사진이 세로로 들어가야 하는데 360에는 안
들어간다.** 그리고 1080p에 정확히 2배, 4K에 4배로 떨어지는 것은 960x540뿐이다(1280x720은
1080p에서 1.5배라 도트가 깨진다).

앞서 "해상도를 올려도 이득이 없다"고 적어둔 것은 **배경 한 장이 화면 전체를 덮던 때** 얘기다.
화면을 정사각형 사진 여러 장으로 채우는 구조가 되면서 전제가 바뀌었다.

**크기와 상관없이 전부 1 generation**이라, 320x320(102,400픽셀) 대신 512x512(262,144픽셀)를
뽑으면 같은 값에 2.5배다. 작게 뽑는 것은 그냥 손해다.

512x512가 갈라지지 않는 것은 실제로 뽑아 확인했다(`assets/photos/obelisk.png`). 옆을 안개와
바위로 채우라고 지시한 것이 먹혔다 — **빈자리를 안 채워주면 모델이 주제를 복제해서 채운다.**

옮기면서 손댄 것: `project.godot`, `DialogueUI`, `BattleScreen`, `LightMenu`, `PhotoStack`,
`Lantern.GLOW_SIZE`, `FilterPreview`, `FilteredBackground`(늘리지 않고 가운데 놓게), 씬 두 개.
좌표가 전부 각 파일 맨 위 상수에 모여 있어서 구조는 안 건드렸다.

**화면을 통째로 덮는 그림은 이제 못 만든다** — 960x540은 pixen의 면적 상한을 넘는다. 그래서
`FilteredBackground`는 늘리지 않고 가운데 놓는다.

---

## 🗺️ 맵 RPG로 틀었다 (2026-08-11, 세션 끝)

**포인트 앤 클릭 대신 피어 앤 헝거처럼 걸어다니는 맵 게임으로 간다.** 회원님이 정하셨다.
일러스트는 잠시 접고 맵부터 만든다.

### 왜 이게 말이 되는가

**오늘 내내 붙들고 있던 벽(960x540짜리 그림을 못 뽑는다)이 통째로 사라진다.**

```
맵       타일을 반복해서 깐다     → 면적 상한과 무관
일러스트  조우·이벤트 때만 띄운다  → 화면을 안 채워도 된다
```

그리고 **맵 쪽은 화풍이 도구로 잡힌다.** 일러스트에서 계속 실패하던 부분이다.

- 타일셋끼리: `lower_base_tile_id`로 앞 타일셋의 타일을 물려받아 이어진다
- 소품: `create_map_object`에 **맵 화면 조각(192x192 이내)** 을 `background_image`로 넘기면
  그 화풍에 맞춰 그려준다. 인페인팅이라 배경이 복제될 일도 없다

**비용도 내가 과장했던 것으로 드러났다.** 문서 확인 결과:

```
탑다운 타일셋 32px    1 gen   (64px는 pro라 20~40)
소품 하나             1 gen
캐릭터 걷기           1 gen / 방향  ← 4방향이면 4 gen
```

기지 하나 만드는 데 대략 10~12 generations다.

### 일 분담 (공식 안내 `pixellab://docs/godot/wang-tilesets`)

```
내가        타일셋 생성 → 내려받기 → 변환 스크립트 → Godot TileSet(.tres) + 씬까지
회원님이    씬 열고 TileMap 탭 → Terrains 탭 → 지형 고르고 → R키로 사각형 드래그
```

**반드시 R(사각형 도구)를 쓴다.** D(붓)는 코너 기반 지형에서 작동하지 않는다고 안내에 못박혀 있다.

### 공식 영상을 보고 설정을 고쳤다

회원님이 [Tilesets 튜토리얼](https://www.youtube.com/watch?v=sE1s5ZYfxqk)과
[high top-down 튜토리얼](https://www.youtube.com/watch?v=q9z2Vhpz-Z8) 자막을 가져다주셨다.
**내가 잡은 값이 세 군데 틀렸다.** 자세한 것은 `CLAUDE.md`의 "타일셋" 절에 정리했다.

```
transition_size   0.25  →  0.5      0.5가 "대부분의 게임에서 보는 표준 벽"
detail            highly → low       32px엔 highly detailed가 안 들어간다
outline           lineless → 켠다    작은 타일은 외곽선이 형태를 잡아준다
묘사              길게 → 두 단어      "marble floor" / "starry void"
```

그리고 **64px은 다른 도구**였다. `high top-down` 타일셋은 16/32뿐이고, 32/48/64/96은 tiles 탭의
새 베타 도구다. 내가 둘을 섞어서 "64 pro로 가자"고 했는데 20~40 gen을 헛되이 태울 뻔했다.

**Godot 경로도 훨씬 쉽다.** MCP 자료엔 변환 스크립트를 쓰라고 나오지만, 영상은 웹에서
"export as tileset 15"로 내보내 **TileMapDual 플러그인**에 드래그하면 끝이라고 보여준다. 코드 0줄.

### 첫 타일셋 — 아카식 기지 바닥

```
공허(별하늘) ↔ 대리석 바닥 · 32px · transition_size 0.25 · lineless · highly detailed
tileset id  b4fc6c67-7a03-410a-a9a5-47cb8c0d0dfb
base tile   공허   013bca70-16c8-41d3-8ffd-b214e93531d4
            대리석 97835612-1928-4c19-a0eb-c28755fecf99
```

**base tile ID는 버리지 말 것.** 다음 타일셋(대리석↔카펫 등)을 이을 때 이게 있어야 같은
대리석이 이어진다.

**[2026-08-14] 이 타일셋은 접었다.** 밝은 흰파랑 대리석에 남색 공허라, 회원님이 주신 서고
참고 그림(따뜻한 호박색 나무와 책, 심연 위의 통로)과 안 맞았다. 아래 ID는 다른 세계에서
쓸 수 있으니 버리지 않는다.

### 서고 타일셋 — 나무 통로 ↔ 심연 (2026-08-14)

```
tileset  15cddff9-7b91-4781-8815-02417fa8f242
심연      93a487f0-3861-4d3b-b844-9badcb48dd51
나무 바닥  a3daef06-de22-4892-ad36-e6be10b9cbb9

lower "endless dark chasm" / upper "old wooden floor"
transition "worn wooden railing" · 32px · transition_size 0.5
single color outline · low detail · basic shading · standard · high top-down
```

지난번과 달라진 것 셋. **`lineless` + `highly detailed`는 §10이 "32px에서 망한다"고 못박은
값이었다.** 그리고 `transition_description`을 처음 써봤다 - 참고 그림의 난간이 여기 들어간다.

### 뽑고 나서 채도를 낮췄다 — `tools/_desat.gd`

받은 그대로는 **너무 쨍했다**(회원님). 재보니 16,384픽셀이 **전부** 채도 0.3을 넘어서,
`DitherFilter`를 통과해도 색이 하나도 안 빠지는 상태였다.

```
원본    평균 0.747   0.3 초과 100%
0.45    평균 0.461   0.3 초과 100%   ← 눈에는 가라앉는데 필터엔 그대로. 소용없다
0.30    평균 0.327   0.3 초과  63%   ← 골랐다
0.15    평균 0.193   0.3 초과   5%
```

**밝기는 그대로 두고 색만 밝기 쪽으로 끌어당긴다.** 어두워지면 채도가 아니라 노출을 만진
것이 되어 형태가 뭉갠다.

원본은 `wood_chasm_raw.png`로 남긴다. `CHOSEN` 값만 바꿔 몇 번이든 다시 돌린다.
**그림을 다시 뽑을 일이 아니었다** - 프롬프트로 채도를 조절하려 했으면 또 헛돈을 썼을 것이다.

그리고 채도를 낮추면 **틴트가 살아난다.** 타일이 제 색을 다 갖고 있을 때는 `tint`를 줘도
안 보이는데, 회색에 가까워지면 얹은 색이 그대로 보인다 - 장소마다 색조를 정할 수 있게 된다.

**[2026-08-14 정정] 아래 "0.5를 넘으면 25장"은 틀렸다.** MCP 도구 설명 기준 standard 모드는
0.0 / 0.25 / **0.5까지 전부 16장**이고 1.0에서만 25장이 된다(pro 모드는 0.5에서 25장).
0.25로 묶여 있을 이유가 없었다 — 자세한 것은 `PIXELLAB.md` 5절.

`transition_size`는 0.25로 잡았다 — 0이면 전환 묘사가 안 먹고, 0.5를 넘으면 타일이 25장이 되어
Godot 변환기가 다른 방식을 써야 한다(16장이어야 코너 기반 지형이 그대로 된다).

### ⚔️ 전투 컨셉이 정해졌다 — 랜턴에서 무기를 뽑는다

회원님이 정하셨다.

```
랜턴에서 무기를 뽑아 쓴다  →  뽑을수록 랜턴이 어두워진다  →  앞이 안 보인다
```

**싸울수록 시야를 잃는다.** 탑다운 맵에서 랜턴은 시야 반경이므로, 무기를 뽑을수록 화면이
좁아진다. 기름 같은 별도 자원이 필요 없다 — **빛 자체가 탄약**이다.

이미 만든 `Lantern.gd`가 그대로 쓰인다. 밝기 5단계와 단계별 시야·명중·화면 밝기가 이미 있으니,
**"기름을 태운다"를 "무기를 뽑는다"로 바꾸기만** 하면 된다.

### 접어둔 것

일러스트 쪽에서 확인한 것들은 남겨둔다 — 나중에 조우/이벤트 그림을 만들 때 그대로 쓴다.

- **512x512가 자유 그림의 천장**이다(면적 상한 262,144). 어떤 변환 경로로도 못 넘는다
- **시드와 파라미터를 고정해도 화풍이 안 잡힌다.** 성당과 피라미드를 같은 설정(seed 1000,
  lineless, highly detailed)으로 뽑았는데 딴판이었다. **소재가 화풍을 끌고 간다**
- 필터는 **팔레트·명암·채도는 잡지만 선 굵기와 묘사 밀도는 못 잡는다.** 회원님 표현으로
  "엄청 다른 게임 같아"

---

## 📖 PixelLab 설명서를 따로 썼다 — `PIXELLAB.md`

공식 영상 일곱 편과 문서를 정리했다. **출처에 우선순위를 박아뒀다** — 영상 > 공식 문서 >
우리 경험. 우리가 겪은 것은 §10에 따로 모았고, 위와 충돌하면 위가 맞다.

이번 세션에 내 짐작이 여러 번 틀렸기 때문이다. 대표적으로:

- "512가 그림 크기의 상한이다" → **아니다.** 웹에서 캔버스를 늘려 조각씩 이어 그릴 수 있다
- "64px 타일은 pro로 20~40 gen" → **아니다.** 도구를 둘 섞어 말했다. high top-down은 16/32뿐
- "화풍은 시드와 파라미터로 잡는다" → **아니다.** 만든 뒤 **팔레트로 강제**하는 것이 공식 답이다
- "맵 RPG는 그림 값이 많이 든다" → **아니다.** V3 애니메이션은 1~8 gen이다

---

---

## 🕯️ 조우 연출을 만들었고, 그게 이 게임의 얼굴이 됐다 (2026-08-11, 다른 컴퓨터)

**처음으로 "이거다" 소리가 나온 화면이다.** 회원님 말씀 그대로 옮기면 *"느낌 개좋은데? 내가
고민하던 게 이걸로 해결된 것 같아."*

| 파일 | |
|---|---|
| `scenes/Walk.tscn` / `scripts/walk/WalkScene.gd` | 조우 연출. 새로 만든 것은 이 둘뿐 |

### 무엇인가

일러스트 한 장이 화면을 채우고, 그림 속 제단에 **그것**이 서 있고, 내가 아래에서 걸어
들어간다. 닿으면 전투가 시작된다.

```
↑↓←→  걷기(끝까지 12초)   [ ]  그것의 발 높이   - =  그것의 크기
Space 다음 사진            D 달리 인   F 채우기   Q 단계로 끊기   G 격자 기준   R 처음부터
```

### 왜 통했는가 (이게 제일 중요하다)

1. **그림이 원근을 이미 그려놨다.** 코드가 하는 일은 점 하나를 위로 밀고 크기를 줄이는 것뿐이다.
   3D도 시차 레이어도 없다
2. **화면을 채운 것.** 320짜리 사진이 검은 화면 가운데 떠 있을 땐 "사진을 보는" 느낌이었는데,
   높이에 맞춰 늘리니 "여기 서 있는" 게 됐다. 이 한 끗이 컸다
3. **느린 속도.** 12초 동안 아무 일도 안 일어나는 것이 외로움을 만든다

### 시점이 정해졌다 — 1점 투시 정면, 카메라는 내 뒤

회원님이 주신 참고 그림(협곡·별밤 수도승·대리석 계단·눈알 피라미드 등)이 **예외 없이 1점
투시**였고, 사람은 전부 **등을 보인 채** 안쪽을 향해 있었다. 등이 보인다는 건 카메라가 옆이
아니라 **내 뒤**에 있다는 뜻이다.

**이래서 옆보기(2D 횡스크롤)를 버렸다.** 옆보기는 깊이 축이 없어서 저 그림들의 힘이 안 나온다.

그리고 **전투 화면이 이미 같은 시점이었다.** `BattleScreen`은 적이 위 가운데(480,176),
등불이 아래 가운데(480,444)다. 걷기와 전투가 카메라를 공유하므로 조우할 때 시점을 안 돌려도 된다.

### 게임 구조가 정해졌다

```
지역 탐색   탑다운   ← 평소
조우/보스   이 연출  ← 어쩌다 한 번
전투        1인칭 턴제 (이미 만들어둔 것)
```

**이 연출을 보스에만 쓰기로 한 것은 회원님 결정이다.** 매 장면이 이러면 무뎌지지만 어쩌다
한 번이면 사건이 된다 — **압도감은 희소해야 산다.**

탑다운이 돌아오면서 타일셋·8방향 캐릭터 비용도 같이 돌아왔다. 다만 **화풍 걱정은 줄었다** —
드퀘도 피어 앤 헝거도 필드와 조우 화면이 딴판이고 아무도 이상하게 안 본다. 시점이 다르면
다른 그림인 게 자연스럽다.

### 겪은 것 세 가지 (다시 겪지 말 것)

- **실루엣이 등불에 지워졌다.** 등불 빛이 `blend_add`라서 검은 사람보다 나중에 그려지면 빛이
  더해져 사람이 사라진다. 첫 캡처에서 사람이 아예 안 보였다. **실루엣을 등불보다 위에** 그린다.
  참고 그림의 협곡도 밝은 빛 **안에** 검은 점이 박힌 구조다
- **디더 격자가 그물처럼 기어다녔다.** 배율이 1.00 → 1.01 → 1.02로 조금씩 변할 때마다 격자가
  다시 짜인다. → **격자를 화면에 못 박아 해결했다**(아래 참고)
- **그것이 배경 위를 미끄러졌다.** 그것한테 배경과 다른 원근 곡선(깊이^2.2)을 줬더니 제단에
  서 있는 게 아니라 따로 놀았다(회원님 지적). → **그것의 자리와 크기를 그림 사각형에 대한
  비율로만** 정한다. 이제 깊이를 아예 안 본다

### 그것은 안 움직이고, 내가 작아진다

**경외감은 그것이 커지는 데서 오는 게 아니라 내가 작아지는 데서 온다.** 협곡 그림이 그렇게 되어
있다. 그래서 내 키는 그림 높이의 16% → 4.5%로 줄고, 그것은 처음부터 끝까지 그 자리에 그대로 크다.

우리가 싸울 것들은 대개 **엄청나게 커다란 무언가**라서(회원님) 이게 성립한다.

그리고 안 움직이는 게 더 무섭다 — 쫓아오면 도망이라도 치는데, 기다리고 있으면 내 발로 가는
수밖에 없다.

### 디더 격자를 화면에 못 박기로 확정했다

`DitherFilter`에 `dither_on_screen` uniform을 넣고 **기본값 true**로 뒀다. 자세한 것은
`CLAUDE.md`의 "고정된 값"에 적어뒀다. 게임 전체에 걸리지만 다른 것들은 전부 1배로 놓여 있어서
보기에 달라지는 것은 없다.

컨셉에도 이쪽이 맞다 — *"색이 있는데 흑백 필터를 낀 느낌"*이라면 필터는 화면에 얹히는 것이지
그림에 붙어 있는 것이 아니다.

### 확인한 것: 적 그림은 다시 안 뽑아도 된다

`hollow_armour`가 "녹슨 주황이 등불과 경쟁하고 게임 에셋처럼 보인다"고 걱정했는데, **필터를
통과시키고 성당 안에 세우니 괜찮다.** 주황이 대부분 빠져 잿빛 강철로 읽히고, 배경과 같은
필터라 오려붙인 티도 안 난다.

---

## 🎨 그림을 실제로 열어보고 알아낸 것 (2026-08-11)

**그동안 문서만 읽고 그림 이야기를 했는데, 이번에 열한 장을 다 열어봤다.**

### 자른 것이 방법이었다

여덟 장이 여덟 장 다 산다. `chapel_1`·`chapel_2`가 제일 좋고(안개·빛줄기·안쪽으로 물러나는
공간), `forest_1`·`forest_2`도 오솔길이 안개로 사라지는 구도가 됐다. `tower_gate_2`는 압도감
규칙 넷 중 셋이 그냥 맞아떨어져 있다.

### 화풍이 세 집안으로 갈린다

```
부드러운 쪽   chapel_1/2, forest_1/2, obelisk    외곽선 없음, 안개로 윤곽이 풀림  ← 목표
외곽선 쪽     tower_1/2, tower_gate_1/2          검은 선이 굵고 색면이 평평함
혼자          pyramid                            얼굴 달린 피라미드, 다른 장르
```

**`obelisk`가 목표에 제일 가깝다.** 회색 바위와 안개인데 갈라진 틈만 붉게 살아 있다 —
"쨍한 것만 색이 남는다"가 필터를 씌우기 전에 원본에서 이미 그렇게 그려져 있다.

### ★ 화풍은 파라미터가 아니라 소재가 끈다

`PIXELLAB.md` §3은 "만든 뒤 팔레트로 강제하라"가 답이라고 하는데, **우리 경우엔 그것도 아니다.**
성당과 피라미드를 같은 설정(seed 1000, lineless, highly detailed)으로 뽑았는데 딴판이었다.

```
회화로 끌리는 것   안개 · 바위 · 구름 · 물 · 빛 · 먼지 · 사막 · 눈보라
만화로 끌리는 것   성당 · 첨탑 · 철책 · 상징물 · 정면을 보는 얼굴
```

픽셀아트에서 고딕 성당은 이미 캐슬바니아고, 눈 달린 피라미드는 이미 만화다. 모델이 그렇게
배웠다. **그러니 "회화처럼"은 형용사가 아니라 무엇을 그리느냐로 정한다.**

그리고 팔레트가 우리한테 덜 중요한 이유가 하나 더 있다 — **화면 필터가 어차피 4계조로 다시
깎는다.** 필터가 팔레트 일을 이미 하고 있다. 못 잡는 것은 **선 굵기와 묘사 밀도**뿐이고,
그건 팔레트로 안 잡힌다.

### `CLAUDE.md`의 "화풍을 붙잡는 법" 절은 낡았다

거기 적힌 "① 모델 고정 ② 시드 고정 ③ 파라미터 고정 ④ 어휘 고정 — 통했던 방법은 이 넷뿐"은
`PIXELLAB.md` §10과 위 관찰로 뒤집혔다. **④(어휘)만 유효하고 ②③은 안 통한다.**

---

## 🐛 아직 안 고친 것

- **`DitherFilter`의 `input_black` / `input_white`가 죽어 있다.** uniform도 있고
  `FilterPreview`에 "흑점/백점" 슬라이더도 붙어 있는데 `fragment()`에서 안 쓴다. 돌려도
  아무 일이 안 일어난다. 주석에 의도는 길게 적혀 있으니 구현만 하면 된다
- **사진 크기가 320·448·512로 섞여 있다.** `PhotoStack.PHOTO_SIZE`는 448로 테두리를 그리는데
  실제 사진 대부분은 320이다
- `chapel448.png`가 하얗게 날아가 있다. 정체 불명(실패한 생성이거나 미리보기 캡처)
- `assets/fonts/Galmuri11.ttf`·`Galmuri9.ttf` 10MB가 안 쓰이고 남아 있다
- `assets/tilesets/void_marble_*`은 `marble_void`의 위아래를 뒤집은 짝인데 아무도 안 쓴다

---

## 다음 세션에 할 일

**앞선 "팔레트 → 주인공 8방향 → 걷기 애니메이션" 계획은 접었다.** 조우 연출로 방향이 바뀌면서
8방향 캐릭터가 당장 필요 없어졌고, 팔레트는 화면 필터가 대신하고 있다.

1. **메인화면 그림을 뽑는다.** 아래 승인 대기 중인 프롬프트가 있다
2. **조우 → 전투 전환.** 회원님이 정하신 순서는 **컷 암전 → 등불 스르르 → 전투 씬**이다.
   `BattleScreen`도 같이 손봐야 한다(캄캄하게 시작해서 등불이 켜지며 적이 드러나야 한다)
3. **그림마다 그것의 발 높이·크기를 맞춰서 데이터로 적어둔다.** `Walk.tscn`에서 `[ ] - =`로
   맞추면 화면에 값이 나온다
4. 실루엣을 제대로 된 뒷모습 그림으로 (지금은 코드로 그린 머리+몸통)

### 메인화면 — 승인 대기 중

거점은 **거대한 금색 고리가 안쪽 빛으로 감겨 들어가는 곳**이다(회원님이 참고 그림 넉 장을
주셨다: 우주 태피스트리 / 나선 코덱스 / 고리 세계 / 검은 태양). 넷의 공통점은
**검은 바탕 + 금색 하나 + 동심원 구조 + 아래쪽 지형**이다.

주의: 그중 **나선 코덱스와 우주 태피스트리는 밀도 자체가 그림**이라 512에 4계조로 깎으면
얼룩이 된다. **고리 세계와 검은 태양** 쪽 구조로 간다.

```
create_image_pixen · 512x512 · lineless · highly detailed · detailed shading · 1 gen

a vast black void, an enormous ring of pale gold light curving away toward a
single bright core far in the distance, concentric bands of tiny glowing
points along the ring, thick banks of dim cloud beneath it, dark jagged
mountain ridges across the bottom edge, deep haze, gold and grey only
```

- `symmetrical`은 안 쓴다(좌우 거울 복제를 부른다). `curving away toward a single core`로 대신한다
- 아래 구름·산등성이는 **빈자리를 안 채워주면 모델이 주제를 복제하기 때문**이고, 동시에
  크기를 알려주는 비교 대상이 된다
- 640x360이 아니라 512 정사각형인 이유: 가로로 길면 좌우로 갈라지는데, **가운데 하나가 중심인
  구도에서는 그게 바로 실패**다. 그리고 배경이 검은 공허라 화면 좌우 여백이 안 보인다 —
  **1배 그대로 쓸 수 있어 격자도 도트도 완벽하다**

메인화면은 조우 연출과 같은 화면으로 만들 수 있다. 그림 + 아래 작은 실루엣 + 등불 +
`LightMenu`의 빛줄기 메뉴, 그리고 **"시작"을 누르면 그 안으로 걸어 들어간다.**
띄울 문장은 이미 정해져 있다 — *"세계를 건너다니는 자에게는 그곳이 온전히 보이지 않는다.
등불이 비추는 것만 실재한다."*

---

**PixelLab MCP를 이 컴퓨터에 붙였다**(2026-08-11). `https://api.pixellab.ai/mcp`, HTTP,
사용자 설정(`~/.claude.json`)에 user 스코프로 등록. **저장소 안 `.mcp.json`에는 넣지 않았다 —
`game_4`가 공개 저장소라 토큰이 그대로 올라간다.**

---

## ⚠️ "색을 가진 것은 등불뿐"은 규칙이 아니다 (2026-08-14 정정)

**회원님이 정하신 적이 없다.** 말이 그렇다는 것이었는데 내가 규칙으로 굳혀서 README·셰이더
주석·코드 주석 여러 곳에 박아놨고, 그걸 근거로 판단까지 했다(적 그림의 주황이 "규칙을
깬다"고 하거나, 타일셋 채도를 문제 삼거나).

아래 옛 항목들에 그 표현이 남아 있는데, **기록이라 지우지 않고 여기 정정만 적어둔다.**

실제로 맞는 것은 이 정도다.

- 화면은 대체로 회색이다 — 필터가 채도 낮은 것을 걷어내기 때문이다
- 등불은 필터 판보다 위에 있어서 제 색을 지킨다
- **그렇다고 다른 것이 색을 가지면 안 되는 것은 아니다.** 스테인드글라스도 비석의 붉은
  균열도 색이 남고, 회원님은 그걸 좋다고 하셨다

색에 대한 판단은 **규칙을 들이대지 말고 그때그때 눈으로** 정한다.

---

## 🚪 관문 그림을 뽑았고, 아웃페인팅에 60 gen을 태웠다 (2026-08-12)

메인화면 겸 출발점으로 **거대한 석조 관문**을 잡았다. 회원님이 주신 참고 그림은 붉은빛이 새어나오는
글자 새겨진 문틀(눈이 달린 것), 문이 겹겹이 이어지는 무한 복도 둘, 그리고 금색 고리 세계다.

### 뽑은 것 — 프롬프트와 시드를 짝으로 남긴다

| 파일 | 시드 | |
|---|---|---|
| `assets/photos/gate_1.png` | **미상** | 첫 시도. 골짜기에 처박혀서 실패 |
| `assets/photos/gate_2.png` | **미상** | 회원님이 "분위기 괜찮다"고 하신 것 |
| `assets/photos/gate_3.png` | **1000** | 가운데 정렬 시도. 검은 하늘은 나왔으나 밋밋해짐 |

`gate_3` 프롬프트 (512x512, 파라미터 없음):

```
a single tall narrow stone doorframe standing exactly in the middle of the picture, centred in
the frame, far taller than it is wide, standing alone on a flat empty plain, seen from low down,
worn glyphs over the stone, the bare flat ground running away to a distant horizon on both sides,
the opening looking through to dark empty distance, black sky, cold grey stone, red light only in
the opening
```

**gate_1·gate_2는 시드를 안 줘서 영영 재현 불가다.** MCP는 결과에 시드를 안 실어 보낸다 —
**호출할 때 직접 지정해야 남는다.** 웹 UI는 생성 후 시드를 알려주고 `Reuse seed` 버튼도 준다.

**`gate_4.png` (2026-08-12, 448x584, 시드 3000, 파라미터 없음)**

`gate_3`과 **똑같은 프롬프트**로 크기만 448x584로 바꿔 뽑았다. 세로를 512보다 72 높인 것은
pixen이 "면적 262,144 이내 · 변 768 이하 · 4의 배수"만 지키면 되므로 가로를 줄이면 세로를
높일 수 있기 때문이다(448x584 = 261,632).

밋밋하지 않게 나왔다 — 소용돌이치는 구름, 돌의 균열로 새는 붉은 빛, 포석, 왼쪽 멀리 지평선의
언덕. 세로가 길어진 만큼 문이 솟아 보인다.

### ★ pixen은 주제를 가운데에 안 놓는다 — 0.63이다

두 장을 실제로 재봤다(밝은 기둥 사이의 중점).

```
gate_2   문 중심 비율 0.632
gate_3   문 중심 비율 0.630   ← 프롬프트에 `centred in the frame`을 넣은 것
gate_4   문 중심 비율 0.62쯤  ← 크기도 시드도 다른데 또 같은 자리 (눈대중)
```

**세 장이 전부 같은 자리다. 우연이 아니라 pixen의 성질이다.**

**말로 시키는 게 아무 효과가 없다.** 시드도 프롬프트도 다른데 어긋난 양이 거의 같다.
그러니 **가운데 정렬에 generation을 쓰지 말고 놓는 자리로 잡는다** — `WalkScene`에 초점(`_focus_x`)을
넣어뒀고 기본값이 0.63이다. 그림·걷는 길·그것의 자리가 이 값 하나로 같이 움직인다.

### 아웃페인팅 실패 — 자세한 것은 `PIXELLAB.md` §10

캔버스를 644x512로 늘려 오른쪽을 이어 그리려다 **세 번 실패, 60 gen 소모.** 원인은
**`use selection tool to select painting area` 옵션을 안 켠 것**으로 보인다. 절차는 `PIXELLAB.md`
§2와 §7에 처음부터 적혀 있었다.

**실패해도 거부되지 않고 20 gen씩 정상 청구된다.** 결과를 반드시 눈으로 확인할 것.

### 자른 판(`gate_2c` · `gate_3c`)은 회원님이 안 쓰기로 하셨다

문을 가운데 맞추려고 왼쪽을 130px쯤 잘라낸 것인데, 잘라내는 방식 자체를 원치 않으셨다.
파일은 남아 있으니 필요 없으면 지우면 된다.

---

## 🚪 여는 화면이 완성됐다 (2026-08-12) — **generation 0**

**메인 씬이 `scenes/Opening.tscn`이다.** 켜면 이 화면이 나온다. 오늘 한 것은 전부 이미 있는
그림을 자르고 늘리고 흔든 것이라 **한 장도 새로 안 뽑았다.**

```
0.0~ 0.8s  캄캄하다
0.8~ 5.8s  여는 글이 한 단어씩 나온다 (11단어, 5초)
5.8~ 6.6s  다 쓰인 글을 그대로 둔다
6.6~ 7.6s  글이 사라진다
7.6~ 8.0s  다시 캄캄하다
8.0~ 8.8s  등불에만 불이 붙는다 — 세계는 아직 안 보인다
8.8~11.4s  그 불에서 빛이 번져 나가 화면을 덮는다
11.4~14.4s 그림이 옆으로 밀리며 관문이 드러난다
```

시간값은 전부 `Opening.gd` 맨 위 상수에 순서대로 모여 있다.

| 파일 | |
|---|---|
| `scenes/Opening.tscn` / `scripts/Opening.gd` | 연출 전부. 시간 하나로 화면을 정해서 아무 시점이나 캡처할 수 있다 |
| `shaders/PortalLeak.gdshader` | 관문에서 세계가 새어 나오는 왜곡. **새로 만든 것** |
| `tools/_cutout.gd` | 문 구멍을 투명하게 도려낸다 |
| `tools/_extend.gd` | 그림을 왼쪽으로 늘린다 |
| `tools/_silhouette.gd` | 등불 든 순례자 실루엣을 찍는다 |
| `assets/photos/archive.png` | 문 너머의 아카식 서고. 512x512, 시드 4000 |
| `assets/photos/pilgrim.png` | 실루엣. 44x80, 키 72px |
| `assets/photos/gate_4_wide.png` | 1260x584. 잘라내고 늘린 최종본 |

`-- --capture`를 붙여 실행하면 다섯 시점을 `tools/_opening_*.png`로 찍고 끝난다.

### ★ 자를 자리는 사람이 칠해주는 쪽이 훨씬 낫다

처음엔 "어두우면 문 안쪽"이라고 밝기로 골라 잘랐다. **하늘과 돌 그림자까지 먹어서** 잘린
범위가 x 201~357, y 22~574였다 — 문이 아니라 그림 절반이다.

회원님이 원본에서 **문 구멍을 순백으로 칠해주셨고**, 흰색만 고르게 바꾸니 한 번에 x 247~318,
y 132~505로 정확히 떨어졌다. 순백은 이 그림에 달리 없는 색이라 오검출이 없다.

**임계값을 붙들고 씨름하는 것보다 마스크를 손으로 칠하는 게 빠르다.**

### 늘리는 폭은 화면 폭 + 미는 거리다

448 + 512 = 960으로 딱 맞췄더니 **미는 동안 그림 왼쪽 끝이 검은 수직선으로 드러났다.**
밀 여유가 없으니 당연한 것이었다. 미는 거리 300을 더 붙여 1260으로 만들어 해결했다.

그리고 **문 구멍 위치를 손으로 옮겨 적던 것을 없앴다.** `_extend.gd`가 투명한 자리를 직접
재서 `Opening.OPENING`과 `SEAM`에 넣을 값을 찍어준다. 잘라내기를 다시 돌릴 때마다 조용히
어긋나던 숫자였다.

```
문 중심   0.631, 0.545   (늘리기 전)
문 중심   0.869, 0.545   ← Opening.OPENING
이음새    0.644          ← Opening.SEAM
```

### 일렁임은 둘로 갈렸다

회원님 주문이 "문 안쪽은 떨리고, 늘어난 쪽은 **거기서 흘러나왔으면** 좋겠다"였는데,
이 둘은 다른 물건이라 셰이더를 나눴다.

| | `PortalWave` (문 안쪽) | `PortalLeak` (늘어난 쪽) |
|---|---|---|
| 방향 | 없음 — 제자리 떨림 | 이음새에서 **바깥으로 진행** |
| 세기 | 균일 | 문 옆 0 → 왼쪽 끝 최대 |
| 늘어남 | 없음 | 세로로 최대 2.2배 |

**세로로 밀기만 해서는 "늘어난다"가 안 된다.** 굼실거릴 뿐이다. 읽는 범위를 좁혀 실제로
확대하는 것(`stretch`)을 같이 넣어야 치즈처럼 늘어진다.

### ★ "끝으로 갈수록 빨리 흐른다"를 속도로 만들면 안 된다

위치마다 속도를 다르게 주면 **시간이 갈수록 위상차가 무한정 벌어져서** 무늬가 점점 잘게
부서진다. 몇 초는 멀쩡해 보이지만 오래 켜두면 망가진다.

대신 **파장을 끝으로 갈수록 길게** 했다(`flow_gain`). 시간 주파수는 어디서나 같으니 파장이
길어진 만큼 마루가 그 배로 빨리 지나간다. 오래 켜둬도 그대로고, 흘러가며 늘어나는 물결이라
보기에도 맞는다. 파장이 거리에 비례해 늘어나므로 **지나온 파장 수가 로그**가 된다.

그리고 시작 지점이 티 나지 않게 두 가지를 했다.
- 나오는 자리를 이음새보다 **0.03 오른쪽**에 둬서 원본 그림 쪽으로도 살짝 넘어가게 했다
- 곧은 경사를 섞었다(`surge`). 끝에서만 몰아치게 두면 **늘어지기 시작하는 자리에 선이 보인다**

### 실루엣은 코드로 찍었다 — 두 번 만에 나왔다

통짜 검정이라 윤곽만 맞으면 되는 그림이다. 세로 위치별 반폭 표 하나로 그린다. 숫자만 고쳐
다시 돌리면 되고 **비용이 0이라 몇 번이고 바꿀 수 있다.**

- **팔을 몸에 붙이면 등불이 실루엣에 먹힌다.** 첫 판에서 옆구리의 혹으로만 보였다.
  관문 쪽으로 내미는 자세로 바꿔 살렸다
- 아랫단을 한 가지 주기로 흔들면 **빗살 같은 톱니**가 된다. 주기가 안 맞는 둘을 겹친다
- 목의 잘록함이 사람으로 읽히게 하는 유일한 단서다. 없으면 종 모양이 된다

**키 72px, 관문 470px.** 이제야 관문이 얼마나 큰지 읽힌다 — 실루엣이 없을 땐 그냥 큰 그림이었다.
그리고 허공에 해처럼 떠 있던 등불이 손으로 갔다(크기도 288 → 128).

등불은 색을 지키려고 필터 판 **위**에 있어서 세계와 같이 안 밀린다. 밀린 만큼을 매 프레임
더해서 손에 붙여 둔다.

### 여는 글을 넣었다

```
세계를 건너다니는 자에게는 그곳이 온전히 보이지 않는다.
등불이 비추는 것만 실재한다.
```

캄캄한 채로 **한 단어씩** 5초에 걸쳐 나오고, 잠깐 머물렀다가 사라진다. 그 다음에야 불이 켜진다.
**글을 먼저 읽히고 세계를 보여주는 순서다.**

**필터 판 위(layer 70)에 놓았다.** 세계에 속한 것이 아니라 세계를 두고 하는 말이라서이기도
하고, 아래에 두면 흐리게 뜨고 지는 동안 글자가 베이어 격자에 잘게 부서진다.

**둘째 줄은 반드시 한 줄로 둔다**(회원님). 이 문장이 게임의 규칙이라 쪼개면 힘이 빠진다.

### ★ 픽셀 글꼴은 크기를 마음대로 못 고른다

32px에서 첫 줄이 880px이라 960 화면에 40px씩만 남는다. "조금만 줄이자"는 주문이 나왔는데
**둥근모는 32 아니면 16뿐이었다** — 네이티브 16의 정수배가 아니면 도트 격자가 어긋난다.
글꼴별로 실제 폭을 재봤다.

```
둥근모   16px  440px      갈무리11  24px  644px
둥근모   32px  880px      갈무리9   30px  804px
```

24px로 줄이려면 글꼴을 바꿔야 해서, **회원님이 32px 유지를 택하셨다.** 앞으로도 글자 크기를
바꾸려면 이 표를 먼저 볼 것 — 중간 크기는 존재하지 않는다.

한 단어씩 드러내는 것은 `visible_characters`로 하는데, **Godot Label은 전체 문장 기준으로
배치를 잡고 뒤쪽 글자만 안 그린다.** 그래서 가운데 정렬이 단어마다 다시 계산되지 않고 이미
나온 단어가 옆으로 밀리지 않는다. 운이 좋았다.

### ★ 빛은 화면 가운데가 아니라 등불에서 나간다

처음엔 `light_position`을 화면 한가운데(0.5, 0.5)에 고정해두고 반경만 키웠다. 사방이 고르게
밝아져서 **빛의 출처가 없었다.** 등불을 순례자 손에 붙이고 나서 그 자리를 광원으로 넘기니
왼쪽 위가 어둡게 남아 등불이 비춘 화면이 됐다.

등불이 화면 오른쪽 아래에 치우쳐 있어서 반경이 1을 넘어야 반대편까지 닿는다(1.05).
**`hint_range`는 편집기 눈금일 뿐이고 코드로 넣는 값은 안 잘린다.**

### ★ 반경을 0으로 줘도 캄캄해지지 않는다

`DitherScreen`의 `lit`은 `1 - smoothstep(radius, radius + softness, 거리)`다. 반경이 0이어도
**번짐의 꼬리가 남아서 광원 자리에 흐린 원반이 계속 보인다.** 번짐도 같이 0으로 줄여야 진짜
어둠이 된다(`maxf(..., 0.001)` — 두 문턱값이 같으면 결과가 정의되지 않는다).

곧바로 불을 켜던 때는 안 보이던 문제다. **어둠 속에서 글을 읽히려고 하니 비로소 드러났다.**

### 다음에 할 것

1. **`scripts/walk/WalkScene.gd`의 `START_PHOTO`가 없는 파일(`gate_3.png`)을 가리킨다.**
   `Walk.tscn`을 열면 그대로 깨진다
2. `scripts/Portal.gd`는 만들어만 두고 **한 번도 안 돌렸다.** `Opening.gd`가 같은 일을 하고
   있어서 당장 필요가 없다 — 탐색 화면에서 관문을 쓸 때 검증한다
3. 여는 화면에서 **"시작"을 누르면 문 안으로 들어가는** 흐름
4. `tools/_opening_*.png`, `_zoom.png` 등 확인용 캡처가 `tools/`에 쌓여 있다

---

## 🧭 5단계로 나눠 만들기로 했다 (2026-08-13) — 회원님이 정하신 것

**여기부터가 게임을 실제로 만드는 계획이다.** 지금까지는 조각을 만들어 본 것이고, 이제
순서를 정해서 이어 붙인다.

| | 결과물 | 끝났다고 볼 기준 |
|---|---|---|
| 1 | 움직이는 주인공 | 지역을 걸어다닌다 |
| 2 | 조우 → 전투 | 무언가를 만나면 전투가 벌어지고 끝난다 |
| 3 | 첫 지역과 그 몬스터들 | 그 지역이 하나의 완결된 장소가 된다 |
| 4 | 레벨 · 능력 · 장비 | 싸운 결과가 캐릭터에 쌓인다 |
| 5 | 메인 씬 UI와 게임 내 UI | 눈에 보이는 것이 다 갖춰진다 |

### 지켜야 할 것 (회원님이 못박으신 것)

- **픽셀랩은 이 다섯 단계를 통틀어 200 generation을 넘지 않는다.** 잔액과는 별개인
  스스로 건 한도다
- **로직과 화면을 가른다.** 회원님 표현으로 "백엔드/프론트엔드". 이미 하고 있는 방식이다 -
  `Battle.gd`는 화면을 모르고 시그널만 쏘고 `BattleScreen.gd`가 받아 그린다.
  대사도 `DialogueController` / `DialogueUI`로 갈라져 있다
- **추가·제거가 쉬워야 한다.** 새 적은 리소스 파일 하나, 새 UI는 화면 쪽만 손대면 되게
- **단계마다 물어보고 진행한다.** 혼자 정하고 나아가지 않는다

### ★ 200을 세는 방법 — 잔액 차이로 세면 안 된다

**계정을 다른 곳에서도 쓰고 계신다**(회원님, 2026-08-13). 그래서 잔액이 줄어든 것이 곧 우리가
쓴 양이 아니다. 실제로 한 번 어긋났다 — 859에서 5를 썼는데 잔액은 842였다(17 감소).

**우리 몫은 호출할 때마다 응답에 찍히는 값을 더해서 센다.** 잔액은 "남았는지"만 보는 데 쓴다.

```
5단계에서 쓴 양   17 / 200   (2026-08-13 현재)

  캐릭터(등불 든 판) 1 + 걷기 8장 4 + 걷기 4장 4      = 9   ← 접었다
  캐릭터(빈손) 1 + 캐릭터(빈손 2) 1                    = 2   ← 접었다
  v3 회전 1 + 템플릿 걷기 4                            = 5   ← 쓰는 것
  그것(watcher) 1                                      = 1   ← 쓰는 것

  **11이 버려졌다.** 등불을 그림에 그려 넣은 것과, 시드 없이 굴려서 맞추려 한 것 탓이다.
  둘 다 위에 왜 그랬는지 적어뒀다.
```

### 200 generation 어림 (검증 안 된 값이다)

일지에 적힌 값에서 가져온 것이고, **실제로 부르기 전에 공식 문서로 확인하고 프롬프트·크기·
비용·잔액을 적어 보여준 뒤 멈춘다.**

```
1 주인공      10~20    8방향 + 걷기
2 조우·전투    0~10    hollow_armour와 사진들이 이미 있다
3 지역·몬스터  20~40    타일셋 2~3개 + 소품 + 몬스터 몇
4 레벨·장비     0~20    아이콘류. 코드로 그리면 0
5 UI           0~20    마찬가지
```

합쳐서 50~110쯤이라 200 안에 여유가 있다. **진짜 위험은 예산이 아니라 재작업이다** -
다시 뽑아도 같은 값이 나가고 실패한 것도 환불이 없다. 뽑기 전에 확정한다.

## 🧍 주인공 스프라이트 — 뽑은 설정 전부 (2026-08-13)

**다른 인물을 같은 화풍으로 뽑을 때 이 표를 그대로 쓴다.** `create_character`에는 시드가
없어서 똑같은 그림은 두 번 안 나온다. 화풍을 맞추는 유일한 수단이 **파라미터와 어휘를
그대로 쓰는 것**이다.

### 캐릭터 — `create_character`, 1 generation

```
a hooded pilgrim, heavy grey travelling cloak with a deep hood, face hidden
in shadow, carrying a small brass lantern in the right hand, worn leather
straps and satchel, bone-white trim, cold grey and dark brown, dark fantasy
```

| 항목 | 값 | 웹 UI에서 부르는 이름 |
|---|---|---|
| mode | `standard` | (기본) |
| size | **32** → 캔버스는 48x48로 나온다 | Canvas size |
| n_directions | **4** | Rotations |
| view | **`high top-down`** | View |
| outline | `selective outline` | Outline |
| shading | `basic shading` | Shading |
| detail | `medium detail` | Detail |
| body_type | `humanoid` | — |
| proportions | 기본 프리셋 | Proportions |
| text_guidance_scale | 8 (기본) | AI freedom(낮게) |

```
결과 id   6f88c0aa-dd74-4c1a-bade-5a863c83f249   (이름: 순례자)
```

**`standard`를 고른 이유는 4방향을 고를 수 있는 유일한 모드라서다.** `pro`(20~40 gen)와
`v3`(2~9 gen)는 **무조건 8방향**이다. 품질은 v3 > pro > standard 순이니, 더 좋게 뽑고 싶으면
v3로 8방향을 뽑고 4개만 쓰는 길이 있다.

**캔버스가 48로 나오는 것은 정상이다.** 애니메이션 여유로 40%쯤 커진다(32 → 48).
타일이 32px이라 인물은 딱 한 칸이다.

### 걷기 — `animate_character`, 방향당 1 generation

```
walking, carrying a lantern at the side
```

| 항목 | 값 |
|---|---|
| mode | **`v3`** |
| directions | south / north / east / west |
| frame_count | 8 → **9장**으로 나온다(기준 1 + 만든 8) |

**템플릿을 안 쓴다.** 공식 영상이 *"물건을 든 캐릭터에는 템플릿을 쓰지 마라 — 템플릿은
맨손 캐릭터로 학습돼서 검 든 기사에 쓰면 이상해진다"*고 못박는다. 등불을 든 우리 주인공이
정확히 여기 해당한다. v3는 비용이 같아서 손해도 없다.

**프레임 수는 이 크기에서 비용에 영향이 없다.** v3 값이 `가로x세로x프레임 ÷ 65536`의
올림이라, 48x48이면 16장을 뽑아도 방향당 1 gen이다. 장수는 비용이 아니라 보기로 정한다.

### 받은 뒤 하는 일 — 전부 0 generation

```
tools/_hood.gd     두건 속 얼굴을 검게 지운다 (살색 + 어둠에 갇힌 밝은 점)
tools/_frames.gd   방향별 프레임을 SpriteFrames로 엮는다 (idle_ / walk_ 로 갈라서)
```

`_frames.gd`의 `WALK_STEP`이 2면 받은 8장에서 한 장 걸러 **4장짜리 걸음**이 된다. 다시 뽑을
필요가 없다 — 걷기 고리는 접지·통과가 균등해서 짝수 번째만 빼도 위상이 맞는다.

### ★ 4장이 8장보다 나았다

처음에 8장으로 뽑았는데 **프레임마다 몸 폭이 흔들려서** 걸을 때 형체가 일렁였다. 4장으로
다시 뽑으니 형태가 안정적이고 다리 교차가 또렷하다. 32px짜리에 8장은 과했다 —
프레임 간 차이가 1픽셀도 안 되는 구간이 생기면 모델이 그 자리를 아무렇게나 채운다.

**한 장 걸러 4장으로 줄이는 것으로는 안 됐다.** 0 generation으로 장수는 줄었지만 자세 자체의
흔들림은 같은 그림이라 그대로 남았다. 결국 4 generation을 더 써서 새로 뽑았다.

```
walk/    8장짜리 (남겨둠)      walk4/   4장짜리 (지금 쓰는 것)
```

### 1단계에서 만든 것 — 로직과 화면을 갈랐다

| 파일 | 하는 일 |
|---|---|
| `scripts/map/Hero.gd` | 자리와 이동 규칙. **화면을 모른다.** 대각선을 여기서 거른다 |
| `scripts/map/HeroSprite.gd` | 방향과 걷는지 여부만 받아 그림을 고른다. **규칙을 모른다** |
| `scripts/map/Walker.gd` | 둘을 엮고 카메라·등불·필터를 맡는다 |
| `tools/_frames.gd` | 프레임을 `SpriteFrames`로 엮는다 |

**서 있는 그림에 generation을 안 썼다.** PixelLab이 걷기를 만들 때 원본 회전 그림을 첫 장으로
끼워 주는데 그게 곧 서 있는 자세다. `idle_` / `walk_`로 갈라두면 방향마다 idle을 따로 뽑을
필요가 없다(4 gen 절약).

### ★ 등불 빛이 캐릭터를 지운다

등불 빛은 **더하기 합성**이고 필터 판보다 위에 그려진다. 그런데 카메라가 나를 따라오니
나는 늘 화면 한가운데에 있고, 빛도 한가운데에 두면 **정통으로 겹쳐서 캐릭터가 통째로 씻긴다.**
조우 연출에서도 같은 것을 겪었다(그때는 실루엣을 빛 위에 그려서 풀었다).

여기서는 **빛을 손 위치로 옮기고 크기를 줄여서**(224 → 128) 풀었다. 방향마다 손 위치가
달라서 `Walker.LANTERN_AT`에 표로 들고 있다.

### ★ 손을 비우고 등불을 따로 얹기로 했다 (2026-08-13 오후)

위의 "등불 든 캐릭터"는 **접었다.** 걷기 프레임마다 등불의 위치와 모양이 튀었고, 물건을 든
캐릭터에는 템플릿 애니메이션도 못 써서 걷는 폼이 나빴다.

```
캐릭터   손을 비운다 → 템플릿 애니메이션이 먹는다 → 걷는 폼이 안정적이다
등불     코드로 찍어서(tools/_lantern.gd) HeroSprite가 따로 얹는다. 0 gen
```

그리고 **등불을 손에 들지 않고 앞에 띄운다**(회원님). *"비밀 결사니까 염력 정도는 쓰겠지."*
손에 매달면 팔 흔들림과 어긋나는데, 띄우면 그 문제가 통째로 사라진다 - 붙어 있을 이유가
없어지니까. 걸음과 **무관한 제 박자로** 위아래 흔든다(`FLOAT_HEIGHT` 2.5 / `FLOAT_SPEED` 1.6).

### ★★ 뽑기를 굴리지 말고 한 장을 정해놓고 돌려라 — v3 + 참조 그림

손을 비우려고 `create_character`를 두 번 더 굴렸는데 **세 번 다 딴사람이 나왔다.** 프롬프트를
거의 그대로 두고 한 구절만 바꿔도 톤이 갈렸다. 시드가 없으니 어휘로 붙잡을 수 있는 범위 밖이다.

답은 **`create_character` + `mode: v3` + `reference_image`** 였다. 도구 설명 그대로 옮기면
*"이 스프라이트를 그대로 여덟 방향으로 돌린다. 그림이 정체성과 화풍을 정하고, 설명은 회전만
안내한다."* 회원님이 손보신 정면 한 장을 넣으니 **같은 사람이 여덟 각도로** 나왔다.

```
create_character  mode=v3  reference_image_base64  size=32  view=high top-down
결과 78f73d44-...  16x32  8방향   비용 1 gen (문서의 2~9보다 쌌다)
```

- **참조는 캔버스를 잘라서 넣는다**(`tools/_reference.gd`). 48x48 캔버스에 담긴 32px 캐릭터를
  그대로 넣으면 결과가 어긋난다고 공식 자료에 적혀 있고, 실제로 여백 없이 16x32로 나왔다
- 비슷하지만 다른 것: **`pro` + `style_character_id`는 "다른 캐릭터, 같은 화풍"**이다.
  적·NPC를 주인공과 같은 화풍으로 만들 때 그쪽을 쓴다
- 걷기는 **템플릿 `walking-4-frames`**, 방향당 1 gen. 손이 비어서 쓸 수 있었고, 앞서 v3로
  뽑았을 때 "1번이 거의 서 있는 자세"였던 문제가 없다
- **템플릿은 4장이 전부 걷기다.** v3처럼 기준 프레임이 안 딸려온다 - 서 있는 그림은 회전
  원본을 따로 가져다 `_frames.gd`의 0번에 놓는다

### 🕯️ 조우를 선으로만 그리기로 했다 (2026-08-13)

`scenes/Encounter.tscn` / `scripts/walk/Encounter.gd`. **검은 화면에 흰 선뿐이다.**

앞선 방식(`Walk.tscn`, 일러스트 한 장을 화면에 채우기)에서 두 가지가 걸렸다. 그것이 배경과
같은 중간 회색이라 **묻혀서 안 보였고**(필터가 4계조로 깎으니 밝기를 올려도 같이 밝아진다),
장소마다 일러스트를 뽑아야 해서 값이 들었다.

선으로만 그리면 둘 다 사라진다. **묻힐 배경이 없고 뽑을 그림도 없다(0 gen).** 회원님이
언더테일 전투 화면을 예로 드셨는데, 그 형식이 우리 규칙(*"색을 가진 것은 등불뿐"*)과 같은
데서 나온 것이다 - 뺄 수 있는 걸 다 빼면 남는 게 그것이다.

- 화면 필터를 **안 씌운다.** 4계조 디더가 1픽셀 흰 선을 갉아먹고, 검정과 흰색뿐이라 할 일도 없다
- **선은 적을수록 낫다.** 16개는 빽빽했다(8개). 검은 자리가 이 화면의 알맹이다
- **그것은 처음부터 크다**(회원님). 점에서 자라나면 "나타나는" 것이 되는데, 그것은 나타나지
  않는다. 처음부터 거기 있었고 내가 다가갈 뿐이다
- 걷는 데 22초. 아무 일도 안 일어나는 시간이 외로움을 만든다
- 주인공은 탑다운 맵의 그것을 **북쪽(뒷모습) 걷기**로 그대로 쓴다

`Walk.tscn`은 안 지웠다. `Walker.ENCOUNTER_SCENE` 한 줄만 되돌리면 일러스트 방식으로 돌아간다.

### 🔭 그것 — 눈으로 뒤덮인 고리

`assets/enemies/watcher.png`. 회원님이 참고 그림을 주셨다(고리마다 눈이 박힌 구체, 성서에
나오는 천사의 모습). **아카식 서고 = 모든 것이 기록된 곳**을 지키는 게 모든 것을 보는 눈인
것은 말이 되고, `archive.png` 한가운데에 이미 고리로 둘러싸인 천구의가 있어서 **그게 깨어난
것처럼** 읽힌다.

```
create_image_pixen  512x512  no_background  lineless  highly detailed  side  seed 7000
비용 1 gen
```

- **pixen에는 시드가 있다.** 캐릭터와 달리 마음에 들면 재현된다. 반드시 지정해서 적어둘 것
- **`no_background`가 안 먹었다.** 켰는데도 배경이 불투명하게 딸려 왔다. `tools/_cutbg.gd`로
  도려냈다 - 색으로 고르면 그것의 어두운 부분까지 먹으니 **가장자리에서 이어진 곳만** 지우고,
  색으로 못 가르는 바깥 테는 **반지름으로** 잘랐다(원형이라 쓸 수 있는 수단이다)
- 원본은 `watcher_raw.png`에 남아 있다

### 🧵 서고 → 조우 → 전투가 한 줄로 이어졌다

`Walker`에 조우 자리를 넣었다(바닥의 가로 82% 지점). **자리를 표시해두지 않는다** - 어쩌다
마주치는 편이 사건이 된다. 조우 → 전투는 `WalkScene`이 이미 하고 있었다.

**깨져 있던 것을 고쳤다**: `WalkScene.START_PHOTO`가 저장소에 없는 `gate_3.png`를 가리키고
있어서 `Walk.tscn`을 열면 그대로 죽었다. 다른 컴퓨터에서 만들고 안 올린 파일로 보인다.

### 조우 화면에 공간을 세웠다 (2026-08-13 저녁) — 전부 0 gen

**원근은 나누기 하나다.** `_post(옆거리, 높이, 깊이)` 하나로 세상 좌표를 화면으로 옮긴다
(`화면 = 소실점 + 세상값 / 깊이`). 바닥·천장·책장·등·정육면체가 전부 이 함수를 쓰고,
원근 계산을 따로 한 곳이 없다. **이걸 먼저 만들었으면 훨씬 빨랐다.**

- **바닥이 있어야 서 있는 자리가 잡힌다.** 소실점을 동심 사각형으로 둘러싸던 것은 복도가
  아니라 터널 정면으로 읽혔다
- **세로선은 안 움직이고 가로선만 다가온다.** 앞으로 곧게 걸을 때 실제로 그렇고, 전부 같이
  움직이면 눈이 안 속는다
- 가로선 간격은 **거리의 역수**다. 균등하게 놓으면 바닥이 눕지 않고 벽처럼 선다
- **책장은 옆면이 있어야 상자가 된다.** 정면 사각형만 그리면 아무리 꾸며도 평면이다
- 책은 **높이가 들쭉날쭉한 세로 막대**다. 틀과 가로줄만으로는 그냥 상자다

### ★ 되풀이해서 걸린 것 — 두 종류의 시간

**걸은 거리(`_flow`)와 시간을 섞어 쓰면 안 된다.** 떠 있는 정육면체의 흔들림을 `_flow`에
물렸더니 **서 있으면 통째로 멎었다.** 다가오는 것은 거리에, 살아 있는 것은 시간에 물린다.

**되감기는 자리를 감춰야 한다.** 깊이가 순환하므로 무엇이든 한 번은 사라진다. 문턱을 두고
끊으면 눈앞에서 툭 꺼진다 - 흐려지며 사라지게 해서 그 순간을 감춘다(책, 정육면체 둘 다).

**씨앗을 슬롯 번호로 뽑으면 안 된다.** `_flow`가 정수를 넘을 때마다 슬롯이 한 칸씩 밀려서
(눈에는 이어져 보인다) 모든 책장의 책이 한꺼번에 갈렸다. `i + floor(_flow)`가 그 책장의
진짜 번호다.

### ★ 잘라낸 것이 분위기였다

`watcher.png` 배경을 도려낼 때 반지름 마스크로 고리 바깥의 옅은 테를 쳐냈다. 배경 찌꺼기로
봤는데 **검은 화면에 올려놓으니 오로라로 읽혔다.** 마스크를 풀고 문턱도 낮춰 되살렸다.

**배경이 검정이면 덜 지워도 손해가 없다.** 남는 것은 어두운 찌꺼기인데 검정 위에서는 안
보이고, 오히려 오로라가 잘리는 쪽이 눈에 띈다.

원본을 `watcher_raw.png`에 남겨둔 덕에 되돌릴 수 있었다. **도려내는 도구는 원본을 건드리지 말 것.**

### ★ 원형에는 세로로 미는 셰이더가 안 맞는다

오로라를 `PortalWave`(세로로 민다)로 흔들었더니 폭을 2에서 16까지 올려도 "일렁인다"가
아니라 **"그림이 흔들린다"**로 보였다. `AuraRipple.gdshader`를 새로 만들었다 - 가운데에서
바깥으로 밀고 물결이 밖으로 흘러나간다.

**가운데는 멎어 있어야 한다**(`calm_radius`). 그래야 물건이 물결치는 게 아니라 거기서 뭔가
뿜어져 나오는 것으로 읽힌다.

그것은 아주 느리게 오르내리고 돈다(한 바퀴 100초). **살아 있는지 아닌지 알 수 없을 만큼**
느려야 한다 - 빠르면 기계가 되고, 멎어 있으면 그림이 된다.

### 🥚 알이 깨지듯 — 조우에서 전투로 (2026-08-13 밤)

```
어둠이 조여든다(깊이 0.22~0.78에 물림)  →  닿음  →  완전 암전
→  까암빡 … 까암빡  →  어둠에서 곧장 빛살이 두두두두 터져 화면을 다 채움
→  조각이 걷히며 화면이 원래대로  →  잠시  →  전투
```

- **두 가지를 같이 쓰면 섞인다.** 깨진 빛살과 흰 판을 겹치니 갈라진 모양이 판에 묻혔다.
  빛살이 터지는 동안 조리개를 같이 연 것도 같은 실수다 — 세상이 비쳐서 빛살과 섞인다.
  그 구간은 **순수한 검정 위에 흰 조각만** 남긴다
- **부풀어 오르는 번쩍을 뺐다.** 빛이 먼저 퍼지고 나서 빛살이 나오면 **빛이 두 번 나는 것**이다
- **일찍 가려야 마지막이 놀랍다.** 0.45부터 가리면 그것이 다 커진 모습을 이미 본 뒤라, 불이
  들어와도 "이미 본 것"이 다시 밝아질 뿐이다. 0.22부터 가려서 **그 사이에 이만큼 커져
  있었다는 걸 마지막에 처음 보게** 한다
- **랜덤처럼 보이되 랜덤이면 안 된다.** 씨앗을 번호에서 뽑되 **두 번 흔들어야** 값이 흩어진다 —
  한 번만 돌리면 이웃한 조각끼리 값도 이웃해서 규칙적인 무늬가 된다

### ★★ 구간이 바뀌는 자리의 값이 이어져야 한다

이번에 **네 번** 걸렸다. 앞 구간이 끝낸 값에서 다음 구간이 시작하지 않으면 툭 튄다.

```
등불 크기   번쩍이 5.5배로 부풀려 놨는데 다음 구간이 0.53배부터 다시 키움 → 빛이 한 번 더
흰 판       번쩍이 0.62까지 씌워 놨는데 다음 구간이 0부터 다시 올림      → 두 번 밝아짐
전환 색     주황이 최대인 채로 자르니 어두운 전투 화면과 낙차가 큼        → 툭 끊김
등불 자리   `lantern_at()`이 이미 더한 `aside`를 밖에서 또 더함           → 빛만 두 번 밀림
```

### ★★ 화면이 안 보이면 값을 만지기 전에 로그부터 — 두 스트림 다

**같은 실수를 두 번 하고 두 번 다 못 봤다.** Godot은 파스 에러를 **표준 에러**로 내보내는데
표준 출력만 읽고 "오류 없이 돌았다"고 말했다. 화면이 새까만 것을 연출 탓으로 알고 값만 만졌다.

```
잘못  Start-Process ... -RedirectStandardOutput $out   → 파스 에러가 안 보인다
맞게  ... -RedirectStandardError $err  로 같이 받고, 먼저 --check-only 로 문법을 본다
```

두 번 다 원인이 같다 — **연출을 다시 짜면서 옛 블록을 안 지우고 새 블록을 얹었다**
(`open` 미선언, `back` 중복 선언).

### 그 밖에

- 원형에는 세로로 미는 셰이더가 안 맞는다 → `AuraRipple.gdshader`(가운데에서 바깥으로)
- 조리개 가장자리는 넓어야 한다(0.22 → 0.9). 또렷하면 기계 장치처럼 읽힌다
- 등불은 **걷는 내내 그대로** 두고 닿기 직전에만 스르르 눌린다. 어둠과 같은 자로 재면
  다가갈수록 내 불이 작아져서 이상하다
- 뒷모습에서 등불을 앞(=화면 위)에 두면 **빛이 머리를 뚫고 나온다.** 어깨 너머로 민다
  (`HeroSprite.aside` — 맵 화면은 안 건드린다)
- 전투 상대를 그것으로(`resources/watcher.tres`), 구도는 **좌우 대칭을 깨서** 그것이 오른쪽
  위로 화면 밖까지 넘치고 나는 왼쪽 아래로 물러난다

### 🎥 다음 할 일 — 카메라 이동으로 전투 들어가기

회원님 주문: **"다 밝아지고 조우 씬에서 카메라가 이동"**해서 전투 구도가 되는 것.

**할 수 있다. 오히려 쉬운 쪽이다** — 조우 화면의 모든 선이 `_post(옆거리, 높이, 깊이)`
하나를 거치므로, **소실점을 옮기면 바닥·책장·천장·그것이 전부 따라 움직인다.** 소실점을
오른쪽 위로 밀면 복도를 비스듬히 보는 그림이 되고, 그게 곧 전투 구도다.

**대가는 씬을 합치는 것이다.** 지금은 `Encounter`와 `Battle`이 갈려 있어서 카메라가 이어질
수 없다. 그게 이 방식의 진짜 비용이고, 설계부터 하고 시작할 것.

### 앞서 적어둔 것

회원님 주문: **"조금 더 기괴하고 압도감이 있으면 좋겠다."** 셋으로 나눴고 서로 안 얽힌다.

1. **일렁임에 물결을 하나 더 겹친다.** 지금은 고른 사인파 하나라 얌전하다. 주기가 다른 둘을
   겹치면 박자가 안 맞아 불규칙하게 꿈틀거린다 — 살아 있는데 짐승은 아닌 것으로 읽힌다
2. **다가갈수록 세상이 걷힌다.** 가까울수록 바닥·책장·천장이 흐려져서 끝에는 **그것과 내
   등불만** 남는다. 지금은 끝까지 복도가 그대로라 도착한 느낌이 없다. **이게 제일 세다**
3. **그것이 화면을 넘어간다.** 지금은 얌전히 화면 안에 들어온다. 끝에 화면 밖으로 나가도록
   키우면 "다 안 보인다"가 되고, 안 보이는 것이 무섭다

그 다음: 선의 밝기를 **등불로부터의 거리**에 물린다. 그러면 *"등불이 비추는 것만 실재한다"*가
장식이 아니라 화면 규칙이 된다.

그리고 서고 맵 타일 — 지금은 바닥뿐이고 벽·책장이 없다. 어림 5 gen

### 겪은 것

- **같은 `action_description`으로 다시 부르면 중복으로 걸러진다.** 과금도 안 되고 아무 일도
  안 일어난다. 다시 뽑으려면 설명을 바꿔야 한다
- **애니메이션은 부를 때의 그림에서 만들어진다.** 웹에서 스프라이트를 고쳐도 이미 돌아간
  작업에는 반영이 안 된다. **고치고 나서 뽑을 것**
- 웹에서 손보면 상태가 하나 더 생길 수 있다(`Idle (copy)`). 원본이 거기 남아 있어서
  되돌릴 때 쓸모가 있었지만, 안 쓸 거면 지워야 나중에 내려받을 때 안 헷갈린다

---

PixelLab 잔액: **859 generations** (2000 중 1141 사용, 2026-08-12 확인).

**오늘 여는 화면 작업은 0 gen이다.** 다만 일지에 적힌 981에서 122가 줄어 있는데 그 사이
내역이 일지에 안 남아 있다 — **뽑을 때마다 여기에 적을 것.**
